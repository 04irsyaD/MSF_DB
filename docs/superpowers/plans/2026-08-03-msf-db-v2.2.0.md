# MSF-DB v2.2.0 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Sumber:** `docs/superpowers/specs/2026-07-31-msf-db-v2-design.md` (seluruh nomor bagian di bawah merujuk ke sana)

**Goal:** Menjadikan antrean job MSF-DB persisten dengan SQLite, menambah rate limiting per IP, mengaktifkan dukungan SQL Server, dan mengeraskan passcode admin — seluruhnya di `v2/msf-db/` tanpa menyentuh `msf-app/`.

**Architecture:** Backend FastAPI mendapat satu komponen persistensi baru (`background/job_store.py`) yang menyimpan metadata job ke SQLite; `JobQueue` tetap menjadi satu-satunya yang menjalankan job dan kini menjembatani memori dengan `JobStore` lewat callback `on_change`. Berkas hasil tetap di disk dengan retensi 60 menit, sedangkan baris riwayat bertahan 30 hari. Rate limiting dipasang sebagai dekorator per-endpoint dengan logika kunci terisolasi di `utils/rate_limit.py`.

**Tech Stack:** Python 3.11, FastAPI 0.115, Pydantic 2.10, SQLAlchemy 2.0, sqlite3 (stdlib), slowapi, pyodbc + msodbcsql18, pytest / pytest-asyncio, Next.js 14.2 + TypeScript 5.7, Docker Compose.

---

## Global Constraints

Setiap task secara implisit memuat seluruh butir di bawah ini.

**Git dan branch**
- Git repository berada di `C:\Users\ROG\Documents\query db` (root repo). Seluruh perintah git dijalankan dari sana, bukan dari `v2/msf-db/`.
- Branch kerja: `dev`. DILARANG commit ke `main`.
- **DILARANG `git push`.** Seluruh push dilakukan manusia (AGENTS.md bagian Critical).
- Satu perubahan kecil satu commit (AGENTS.md aturan 5). Satu task di plan ini menghasilkan tepat satu commit.

**Direktori**
- Seluruh perubahan kode berada di `v2/msf-db/`. `msf-app/` adalah referensi baca-saja dan TIDAK BOLEH disentuh.
- `v2/msf-db/ai-rules/` IMMUTABLE — hanya dibaca, tidak pernah diubah.
- Dokumentasi output (`planning/`, `dev-docs/`, `reports/`) berada di `v2/msf-db/` sebagai project root, paralel dengan `backend/` dan `frontend/` — JANGAN dipindahkan ke dalam folder kode.
- Pengecualian resmi: `v2/msf-db/backend/docs/operations/` berada di dalam folder kode karena harus ikut ter-deploy.

**Gaya dan standar**
- DILARANG emoji/ikon di kode maupun dokumentasi. Pengecualian tunggal: tanda centang dan silang untuk checklist di dokumentasi.
- Seluruh komentar, pesan error, dan dokumentasi ditulis dalam Bahasa Indonesia, mengikuti kode yang ada.
- Batas ukuran berkas (`ai-rules/coding-standards/01-file-size-limits.md`): Controller maks 1000, Service maks 800, Model maks 300, Route maks 200, View split di atas 500.
- `routers/generate.py` SUDAH melanggar batas route (328 baris terhadap batas 200) sebelum rilis ini. Dilaporkan di task report, TIDAK diperbaiki di rilis ini (spec 12.1).

**Keamanan**
- Seluruh secret di `.env`. DILARANG hardcode kredensial di kode.
- Setiap key baru di `.env` WAJIB punya placeholder di `.env.example` pada batch yang sama (AGENTS.md aturan 3).
- DILARANG menulis kredensial aktual di berkas `.md` di dalam folder kode (`backend/`, `frontend/`) — hanya boleh merujuk `.env`.
- Mengubah `.env` memerlukan izin eksplisit user. Task 2 memuat gerbang persetujuan untuk ini.

**Kontrak yang tidak boleh berubah (spec 8.5)**
1. Bentuk kode akses tetap `MSF-` + 10 heksadesimal huruf besar.
2. Polling tetap `GET /api/jobs/{job_id}` dengan bentuk `JobStatusResponse` yang sama.
3. `download_url` hanya terisi bila berkas benar-benar ada di disk.
4. Pemetaan progres 0-90-92-100 tidak berubah.
5. Status tetap lima: `queued`, `processing`, `done`, `error`, `cancelled`.
6. Gerbang `MAX_CONCURRENT_JOBS` tetap dihitung dari memori, bukan dari basis data.
7. Kredensial database tetap tidak pernah disimpan dan tetap disensor di log.
8. Jumlah endpoint tetap 22 — tidak ada yang ditambah maupun dihapus.

**Verifikasi wajib sebelum layak merge**

```bash
cd "C:/Users/ROG/Documents/query db/v2/msf-db/backend" && python -m pytest -v
```

```bash
cd "C:/Users/ROG/Documents/query db/v2/msf-db/frontend" && npm run lint && npm run build
```

```bash
cd "C:/Users/ROG/Documents/query db/v2/msf-db" && docker compose config
```

---

## File Structure

| Berkas | Status | Tanggung jawab | Task |
|---|---|---|---|
| `v2/msf-db/backend/**`, `frontend/**`, `dev-docs/**`, `planning/**` | Salinan baseline | Titik awal identik dengan msf-app | 1 |
| `v2/msf-db/.env`, `.env.example`, `docker-compose.yml` | Ubah | Isolasi port, container, volume; config drift | 2 |
| `backend/app/routers/admin.py` | Ubah | Passcode dengan `compare_digest`; statistik dari JobStore | 3, 6 |
| `backend/conftest.py` | Ubah | Isolasi singleton `job_queue` dan SQLite per test | 4, 5 |
| `backend/app/background/job_store.py` | Baru | Baca/tulis baris job ke SQLite; rekonsiliasi; dua pembersihan | 5 |
| `backend/app/background/job_queue.py` | Ubah | Callback persistensi, rehidrasi, dua pembersihan | 5, 6 |
| `backend/app/main.py` | Ubah | Rekonsiliasi di lifespan; loop pembersihan; registrasi limiter | 5, 6, 7 |
| `backend/app/routers/generate.py` | Ubah | 410 RESULT_EXPIRED; `error_code` 429; dekorator rate limit | 6, 7 |
| `backend/app/utils/rate_limit.py` | Baru | Kunci pembatas, pembacaan limit dari env, handler 429 | 7 |
| `frontend/src/lib/api.ts` | Ubah | Membedakan dua jenis 429 dan menangani 410 | 8 |
| `backend/app/services/db_connector.py`, `Dockerfile`, `requirements.txt` | Ubah | Dukungan SQL Server | 9 |
| `backend/docs/operations/*.md` | Baru | Dokumentasi operations (3 berkas) | 10 |
| `dev-docs/**`, `planning/**`, `reports/task/**`, `claude.md`, `TODO.md` | Ubah/Baru | Sinkronisasi dokumentasi wajib | 10 |

**Keputusan lokasi spesifikasi (spec 15.2).** Spesifikasi TETAP berada di `docs/superpowers/specs/` dan isinya didistilasi ke `v2/msf-db/planning/` serta `v2/msf-db/dev-docs/` pada Task 10. Alasan: `docs/superpowers/` adalah artefak alur kerja perencanaan di root repo, sedangkan `planning/` adalah artefak proyek yang diatur AGENTS.md. Bila user memilih sebaliknya, hanya Task 10 yang terdampak.

---

## Task 1: Baseline — salin msf-app ke v2/msf-db

Spec 11.0, D1. Diff perbaikan harus bersih dari diff penyalinan, jadi penyalinan berdiri sebagai commit sendiri.

**Files:**
- Create: `v2/msf-db/backend/**` (salinan dari `msf-app/backend/`)
- Create: `v2/msf-db/frontend/**` (salinan dari `msf-app/frontend/`)
- Create: `v2/msf-db/dev-docs/**` (salinan dari `msf-app/dev-docs/`)
- Create: `v2/msf-db/planning/**` (salinan dari `msf-app/planning/`)

**Interfaces:**
- Consumes: —
- Produces: seluruh pohon kode yang dipakai Task 2 sampai 10. Setelah task ini, `v2/msf-db/backend/app/background/job_queue.py` berisi `Job`, `JobQueue`, dan singleton `job_queue` versi v2.1.0.

- [ ] **Step 1: Pastikan berada di branch dev dengan working tree bersih**

```bash
cd "C:/Users/ROG/Documents/query db" && git status --short && git branch --show-current
```

Expected: branch `dev`. Bila ada perubahan lokal selain `docs/superpowers/`, commit atau stash dulu (AGENTS.md bagian 2a).

- [ ] **Step 2: Salin empat direktori tanpa artefak build**

```bash
cd "C:/Users/ROG/Documents/query db" && for d in backend frontend dev-docs planning; do rsync -a --exclude 'node_modules' --exclude '__pycache__' --exclude '.next' --exclude '.pytest_cache' --exclude 'outputs' --exclude '.venv' --exclude '*.pyc' "msf-app/$d/" "v2/msf-db/$d/"; done
```

- [ ] **Step 3: Verifikasi hasil salinan**

```bash
cd "C:/Users/ROG/Documents/query db" && ls v2/msf-db/backend/app/background/ && ls v2/msf-db/frontend/src/lib/ && find v2/msf-db -name "node_modules" -o -name "__pycache__" -o -name ".next" | head
```

Expected: `job_queue.py` dan `__init__.py` terlihat, `api.ts` dan `types.ts` terlihat, dan perintah `find` tidak mengeluarkan satu baris pun.

- [ ] **Step 4: Verifikasi test suite baseline lulus sebelum disentuh**

```bash
cd "C:/Users/ROG/Documents/query db/v2/msf-db/backend" && python -m pytest -q
```

Expected: PASS. Bila gagal, catat keluarannya apa adanya dan hentikan — baseline yang tidak hijau membuat kegagalan task berikutnya tidak dapat dibedakan.

- [ ] **Step 5: Commit**

```bash
cd "C:/Users/ROG/Documents/query db" && git add v2/msf-db && git commit -m "chore: salin baseline msf-app ke v2/msf-db"
```

---

## Task 2: Isolasi sumber daya v2 dan pembenahan config drift

Spec 11.1, 11.6, D4, D5, 9.9, 9.10, 9.12, D-2, D-3.

**Files:**
- Modify: `v2/msf-db/docker-compose.yml`
- Modify: `v2/msf-db/.env.example`
- Modify: `v2/msf-db/.env` (memerlukan izin eksplisit user)

**Interfaces:**
- Consumes: pohon berkas dari Task 1.
- Produces: variabel env `JOBS_DB_PATH`, `JOB_RECORD_RETENTION_DAYS`, `RATE_LIMIT_ENABLED`, `RATE_LIMIT_GENERATE`, `RATE_LIMIT_ADMIN_VERIFY`, `RATE_LIMIT_TRUST_FORWARDED_FOR`, `MSSQL_ODBC_DRIVER`, `MSSQL_TRUST_SERVER_CERTIFICATE` — dipakai Task 5, 6, 7, dan 9.

- [ ] **Step 1: Minta izin eksplisit user untuk mengubah `.env`**

AGENTS.md aturan 3 melarang mengubah `.env` tanpa izin eksplisit. Tanyakan sekali, sebutkan daftar key yang akan ditambah/diubah, dan tunggu jawaban sebelum melanjutkan. Bila user menolak, kerjakan hanya `.env.example` dan `docker-compose.yml`, lalu catat di task report bahwa `.env` harus diisi manual oleh user.

- [ ] **Step 2: Ubah `container_name` dan nama volume di `docker-compose.yml`**

Ganti empat `container_name`: `msf-backend` → `msf2-backend`, `msf-frontend` → `msf2-frontend`, `msf-postgres` → `msf2-postgres`, `msf-cloudflared` → `msf2-cloudflared`.

Pada blok `volumes:` di bagian bawah berkas, ganti ketiga nama volume:

```yaml
volumes:
  # Dideklarasikan untuk kelengkapan, TIDAK terpakai: layanan ollama dinonaktifkan
  # dan dijalankan langsung di host karena bentrok port.
  ollama_data:
    name: msf2_ollama_data
  postgres_data:
    name: msf2_postgres_data
  backend_outputs:
    name: msf2_backend_outputs
```

- [ ] **Step 3: Jadikan `OLLAMA_BASE_URL`, `CORS_ORIGINS`, dan passcode benar-benar dapat dikonfigurasi**

Pada blok `environment:` layanan `backend` di `docker-compose.yml`, ganti empat baris berikut:

```yaml
      - OLLAMA_BASE_URL=${OLLAMA_BASE_URL:-http://host.docker.internal:11435}
      - ADMIN_PASSCODE=${ADMIN_PASSCODE:-}
      - CORS_ORIGINS=${CORS_ORIGINS:-http://localhost:3001,http://127.0.0.1:3001}
```

dan HAPUS seluruh baris `- SECRET_KEY=${SECRET_KEY:-dev-secret-key-change-in-production}` (tidak ada kode yang membacanya, spec 9.11).

Tambahkan variabel baru pada blok `environment:` yang sama:

```yaml
      - JOBS_DB_PATH=${JOBS_DB_PATH:-/app/outputs/jobs.db}
      - JOB_RECORD_RETENTION_DAYS=${JOB_RECORD_RETENTION_DAYS:-30}
      - RATE_LIMIT_ENABLED=${RATE_LIMIT_ENABLED:-true}
      - RATE_LIMIT_GENERATE=${RATE_LIMIT_GENERATE:-10/minute}
      - RATE_LIMIT_ADMIN_VERIFY=${RATE_LIMIT_ADMIN_VERIFY:-5/minute}
      - RATE_LIMIT_TRUST_FORWARDED_FOR=${RATE_LIMIT_TRUST_FORWARDED_FOR:-false}
      - MSSQL_ODBC_DRIVER=${MSSQL_ODBC_DRIVER:-ODBC Driver 18 for SQL Server}
      - MSSQL_TRUST_SERVER_CERTIFICATE=${MSSQL_TRUST_SERVER_CERTIFICATE:-yes}
```

- [ ] **Step 4: Perbarui `.env.example`**

Tetapkan nilai port v2 dan dokumentasikan seluruh variabel. Tambahkan blok berikut, dan pastikan setiap variabel yang sudah ada di berkas diperbarui nilainya, bukan diduplikasi:

```dotenv
# --------------------------------------------------------------
# PORT v2 — dipisah agar dapat berjalan bersamaan dengan msf-app
# --------------------------------------------------------------
BACKEND_PORT=8001
FRONTEND_PORT=3001
POSTGRES_PORT=5433
OLLAMA_BASE_URL=http://host.docker.internal:11435
CORS_ORIGINS=http://localhost:3001,http://127.0.0.1:3001
NEXT_PUBLIC_API_URL=http://localhost:8001

# --------------------------------------------------------------
# VARIABEL YANG SEBELUMNYA TIDAK TERDOKUMENTASI (spec 9.10)
# --------------------------------------------------------------
# Admin Portal NONAKTIF selama nilai ini kosong. Tidak ada nilai default.
ADMIN_PASSCODE=
JOB_TIMEOUT_SECONDS=1800
LOG_FILE_PATH=/app/outputs/app.log
SHORTCUTS_DIR=/app/shortcuts_data
TEMPLATES_DIR=/app/templates
DEEPSEEK_TIMEOUT=120
OPENAI_TIMEOUT=120
# Rahasia. Wajib diisi bila layanan cloudflared dipakai.
CLOUDFLARE_TUNNEL_TOKEN=

# --------------------------------------------------------------
# PERSISTENSI JOB (v2.2.0)
# --------------------------------------------------------------
JOBS_DB_PATH=/app/outputs/jobs.db
# Umur berkas hasil di disk, dalam menit. Baris riwayat TIDAK ikut terhapus.
MAX_JOB_RETENTION_MINUTES=60
# Umur baris riwayat job, dalam hari.
JOB_RECORD_RETENTION_DAYS=30

# --------------------------------------------------------------
# RATE LIMITING (v2.2.0)
# --------------------------------------------------------------
RATE_LIMIT_ENABLED=true
RATE_LIMIT_GENERATE=10/minute
RATE_LIMIT_ADMIN_VERIFY=5/minute
# Nyalakan HANYA bila backend berada di belakang proxy tepercaya.
# Header X-Forwarded-For dapat dipalsukan klien.
RATE_LIMIT_TRUST_FORWARDED_FOR=false

# --------------------------------------------------------------
# SQL SERVER (v2.2.0)
# --------------------------------------------------------------
MSSQL_ODBC_DRIVER=ODBC Driver 18 for SQL Server
MSSQL_TRUST_SERVER_CERTIFICATE=yes

# --------------------------------------------------------------
# TIDAK DIPAKAI KODE MANA PUN — dipertahankan sebagai catatan (spec 9.10)
# SECRET_KEY, MAX_OUTPUT_SIZE_MB, DEFAULT_OUTPUT_FORMAT, LOG_LEVEL,
# LOG_FORMAT, OLLAMA_DEFAULT_MODEL, TEST_POSTGRES_URL
# --------------------------------------------------------------
```

- [ ] **Step 5: Samakan `.env` dengan `.env.example` (hanya bila user mengizinkan di Step 1)**

Salin seluruh key di atas ke `v2/msf-db/.env` dengan nilai yang sama, KECUALI `ADMIN_PASSCODE` dan `CLOUDFLARE_TUNNEL_TOKEN` yang diisi user sendiri.

- [ ] **Step 6: Verifikasi compose valid dan tidak ada nama msf-app tersisa**

```bash
cd "C:/Users/ROG/Documents/query db/v2/msf-db" && docker compose config
```

Expected: keluar tanpa error, dan pada keluarannya nama container adalah `msf2-*` serta volume `msf2_*`.

```bash
cd "C:/Users/ROG/Documents/query db/v2/msf-db" && grep -nE "msf-(backend|frontend|postgres|cloudflared)|msf_(ollama|postgres|backend)_|admin123|dev-secret-key" docker-compose.yml .env.example
```

Expected: tidak ada keluaran sama sekali.

- [ ] **Step 7: Commit**

```bash
cd "C:/Users/ROG/Documents/query db" && git add v2/msf-db/docker-compose.yml v2/msf-db/.env.example && git commit -m "chore: isolasi sumber daya v2 dan rapikan config drift"
```

Catatan: `.env` diabaikan `.gitignore` sehingga tidak masuk staging — ini disengaja, dan itulah alasan `.env.example` wajib ikut diubah.

---

## Task 3: Pengerasan passcode admin

Spec 11.5, 9.11, D6, NFR-S2.

**Files:**
- Modify: `v2/msf-db/backend/app/routers/admin.py:32`
- Test: `v2/msf-db/backend/tests/test_admin.py`

**Interfaces:**
- Consumes: `docker-compose.yml` dari Task 2 yang sudah tidak menyuntikkan `admin123`.
- Produces: `verify_admin_passcode()` dengan perbandingan waktu-konstan. Dipakai Task 7 sebagai endpoint ber-rate-limit.

- [ ] **Step 1: Tulis test yang gagal**

Tambahkan di `v2/msf-db/backend/tests/test_admin.py`:

```python
def test_passcode_salah_ditolak_dengan_401(monkeypatch):
    """Passcode yang salah harus ditolak lewat perbandingan waktu-konstan."""
    import app.routers.admin as admin_module
    monkeypatch.setattr(admin_module, "ADMIN_PASSCODE", "passcode-benar")

    from fastapi.testclient import TestClient
    from app.main import app

    with TestClient(app) as c:
        response = c.post("/api/admin/verify", headers={"X-Admin-Passcode": "passcode-salah"})

    assert response.status_code == 401


def test_verify_memakai_compare_digest():
    """Perbandingan passcode tidak boleh memakai operator != yang bocor waktu."""
    import inspect
    import app.routers.admin as admin_module

    source = inspect.getsource(admin_module.verify_admin_passcode)
    assert "compare_digest" in source
```

- [ ] **Step 2: Jalankan test untuk memastikan gagal**

```bash
cd "C:/Users/ROG/Documents/query db/v2/msf-db/backend" && python -m pytest tests/test_admin.py -v
```

Expected: `test_verify_memakai_compare_digest` FAIL dengan `AssertionError`.

- [ ] **Step 3: Terapkan `compare_digest`**

Pada `v2/msf-db/backend/app/routers/admin.py`, tambahkan `import secrets` di bagian atas berkas, lalu ganti blok pemeriksaan pada baris 32:

```python
    if not secrets.compare_digest(x_admin_passcode, ADMIN_PASSCODE):
        raise HTTPException(
            status_code=401,
            detail="Admin passcode tidak valid."
        )
```

- [ ] **Step 4: Koreksi komentar keliru pada test yang sudah ada**

Pada `v2/msf-db/backend/tests/test_admin.py:14` terdapat komentar yang menyatakan env dibaca dinamis. Itu keliru: `admin.py:15` membaca `ADMIN_PASSCODE` saat modul di-import (spec 9.8). Ganti komentar tersebut menjadi:

```python
# ADMIN_PASSCODE dibaca sekali saat modul admin.py di-import, bukan per request.
# Karena itu test menambal atribut modul (monkeypatch.setattr), bukan variabel environment.
```

- [ ] **Step 5: Jalankan test untuk memastikan lulus**

```bash
cd "C:/Users/ROG/Documents/query db/v2/msf-db/backend" && python -m pytest tests/test_admin.py -v
```

Expected: PASS seluruhnya.

- [ ] **Step 6: Commit**

```bash
cd "C:/Users/ROG/Documents/query db" && git add v2/msf-db/backend/app/routers/admin.py v2/msf-db/backend/tests/test_admin.py && git commit -m "fix: hapus passcode admin default dan pakai compare_digest"
```

---

## Task 4: Isolasi singleton job_queue di test

Spec 9.7, 17. Task ini WAJIB mendahului Task 5: tanpa isolasi, SQLite dengan default path `/app/outputs/jobs.db` membuat seluruh suite gagal di Windows.

**Files:**
- Modify: `v2/msf-db/backend/conftest.py`
- Test: `v2/msf-db/backend/tests/test_conftest_isolation.py` (baru)

**Interfaces:**
- Consumes: `JobQueue` dari Task 1.
- Produces: fixture autouse `isolated_job_queue` yang mengembalikan instance `JobQueue` bersih per test dan menambal seluruh modul yang mengimpor singleton. Task 5, 6, dan 7 memperluas fixture ini.

- [ ] **Step 1: Tulis test yang gagal**

Buat `v2/msf-db/backend/tests/test_conftest_isolation.py`:

```python
"""Membuktikan setiap test mendapat job_queue yang bersih."""


def test_a_membuat_job(isolated_job_queue):
    isolated_job_queue.create_job(project_name="Test A")
    assert len(isolated_job_queue.list_jobs()) == 1


def test_b_tidak_melihat_job_dari_test_a(isolated_job_queue):
    assert isolated_job_queue.list_jobs() == []


def test_router_memakai_instance_yang_sama(isolated_job_queue):
    import app.routers.generate as generate_router
    import app.routers.admin as admin_router

    assert generate_router.job_queue is isolated_job_queue
    assert admin_router.job_queue is isolated_job_queue
```

- [ ] **Step 2: Jalankan test untuk memastikan gagal**

```bash
cd "C:/Users/ROG/Documents/query db/v2/msf-db/backend" && python -m pytest tests/test_conftest_isolation.py -v
```

Expected: FAIL dengan `fixture 'isolated_job_queue' not found`.

- [ ] **Step 3: Tambahkan fixture di `conftest.py`**

Tambahkan di `v2/msf-db/backend/conftest.py`, setelah fixture `event_loop`:

```python
@pytest.fixture(autouse=True)
def isolated_job_queue(tmp_path, monkeypatch):
    """
    Setiap test mendapat singleton job_queue sendiri dengan OUTPUT_DIR sementara.

    Modul router mengimpor singleton lewat 'from ... import job_queue', sehingga
    menambal modul asal saja tidak cukup — setiap modul pengimpor ikut ditambal.
    """
    output_dir = tmp_path / "outputs"
    output_dir.mkdir()
    monkeypatch.setenv("OUTPUT_DIR", str(output_dir))

    import app.background.job_queue as jq_module
    monkeypatch.setattr(jq_module, "OUTPUT_DIR", str(output_dir))

    fresh = jq_module.JobQueue(max_retention_minutes=60)
    monkeypatch.setattr(jq_module, "job_queue", fresh)

    import app.routers.admin as admin_router
    import app.routers.generate as generate_router
    import app.main as main_module

    monkeypatch.setattr(admin_router, "job_queue", fresh)
    monkeypatch.setattr(generate_router, "job_queue", fresh)
    monkeypatch.setattr(main_module, "job_queue", fresh)

    yield fresh
```

- [ ] **Step 4: Jalankan test untuk memastikan lulus**

```bash
cd "C:/Users/ROG/Documents/query db/v2/msf-db/backend" && python -m pytest tests/test_conftest_isolation.py -v
```

Expected: 3 PASS.

- [ ] **Step 5: Jalankan seluruh suite untuk memastikan tidak ada regresi**

```bash
cd "C:/Users/ROG/Documents/query db/v2/msf-db/backend" && python -m pytest -q
```

Expected: PASS seluruhnya.

- [ ] **Step 6: Commit**

```bash
cd "C:/Users/ROG/Documents/query db" && git add v2/msf-db/backend/conftest.py v2/msf-db/backend/tests/test_conftest_isolation.py && git commit -m "test: tambah isolasi job_queue di conftest"
```

---

## Task 5: JobStore SQLite, persistensi, dan rekonsiliasi job yatim

Spec 11.2 (bagian penyimpanan dan rekonsiliasi), 6.2, 9.1, 9.4, D8.

**Files:**
- Create: `v2/msf-db/backend/app/background/job_store.py`
- Modify: `v2/msf-db/backend/app/background/job_queue.py`
- Modify: `v2/msf-db/backend/app/main.py:72-101` (lifespan)
- Modify: `v2/msf-db/backend/conftest.py`
- Test: `v2/msf-db/backend/tests/test_job_store.py` (baru)
- Test: `v2/msf-db/backend/tests/test_job_queue.py` (diperluas)

**Interfaces:**
- Consumes: fixture `isolated_job_queue` dari Task 4.
- Produces:
  - `JobStore(db_path: Optional[str] = None)` dengan metode `save(row: dict) -> None`, `get(job_id: str) -> Optional[dict]`, `get_by_access_code(access_code: str) -> Optional[dict]`, `query(limit: int = 500) -> List[dict]`, `delete(job_id: str) -> bool`, `reconcile_orphans() -> int`, `list_hydratable() -> List[dict]`, `list_purgeable_files(max_age_minutes: int) -> List[Tuple[str, str]]`, `mark_file_purged(job_id: str) -> None`, `purge_expired_records(retention_days: int) -> int`, `close() -> None`
  - Konstanta modul `RESTART_ERROR_MESSAGE: str`, `SCHEMA_VERSION: int`, `COLUMNS: Tuple[str, ...]`
  - `Job.__init__(job_id, project_name="Database Documentation", on_change: Optional[Callable[["Job"], None]] = None)`, atribut baru `Job.file_purged: bool`, metode `Job.to_row() -> dict`, classmethod `Job.from_row(row: dict) -> "Job"`
  - `JobQueue(max_retention_minutes: Optional[int] = None, store: Optional[JobStore] = None)`, metode baru `JobQueue.recover() -> int`, `JobQueue.close() -> None`, properti `JobQueue.store -> JobStore`
- Dipakai Task 6 (`purge_expired_files`, `file_purged`) dan Task 10 (dokumentasi).

- [ ] **Step 1: Tulis test JobStore yang gagal**

Buat `v2/msf-db/backend/tests/test_job_store.py`:

```python
"""Test untuk JobStore — penyimpanan job persisten berbasis SQLite."""

import os
from datetime import datetime, timedelta, timezone

import pytest

from app.background.job_store import RESTART_ERROR_MESSAGE, JobStore


def _row(job_id: str, **overrides) -> dict:
    now = datetime.now(timezone.utc).isoformat()
    row = {
        "job_id": job_id,
        "access_code": f"MSF-{job_id.upper()}",
        "project_name": "Proyek Uji",
        "status": "done",
        "progress": 100,
        "tables_total": 2,
        "tables_processed": 2,
        "current_table": None,
        "created_at": now,
        "updated_at": now,
        "completed_at": now,
        "error_message": None,
        "preview_markdown": "# Judul",
        "result_filepath": None,
        "result_filename": "dokumentasi.docx",
        "output_format": "docx",
        "ai_provider": "ollama",
        "db_engine": "ddl",
        "file_purged": 0,
    }
    row.update(overrides)
    return row


@pytest.fixture
def store(tmp_path):
    s = JobStore(db_path=str(tmp_path / "jobs.db"))
    yield s
    s.close()


def test_save_lalu_get_mengembalikan_baris_yang_sama(store):
    store.save(_row("job-1"))

    hasil = store.get("job-1")

    assert hasil is not None
    assert hasil["job_id"] == "job-1"
    assert hasil["project_name"] == "Proyek Uji"
    assert hasil["progress"] == 100


def test_get_pada_job_yang_tidak_ada_mengembalikan_none(store):
    assert store.get("tidak-ada") is None


def test_get_by_access_code(store):
    store.save(_row("job-1", access_code="MSF-ABCDEF0123"))

    hasil = store.get_by_access_code("MSF-ABCDEF0123")

    assert hasil is not None
    assert hasil["job_id"] == "job-1"


def test_save_kedua_kali_menimpa_baris_yang_sama(store):
    store.save(_row("job-1", progress=10))
    store.save(_row("job-1", progress=90))

    assert store.get("job-1")["progress"] == 90
    assert len(store.query()) == 1


def test_data_bertahan_setelah_store_ditutup_dan_dibuka_ulang(tmp_path):
    db_path = str(tmp_path / "jobs.db")
    pertama = JobStore(db_path=db_path)
    pertama.save(_row("job-1"))
    pertama.close()

    kedua = JobStore(db_path=db_path)
    try:
        assert kedua.get("job-1") is not None
    finally:
        kedua.close()


def test_reconcile_orphans_mengubah_job_aktif_menjadi_error(store):
    store.save(_row("job-queued", status="queued", completed_at=None))
    store.save(_row("job-processing", status="processing", completed_at=None))
    store.save(_row("job-done", status="done"))

    jumlah = store.reconcile_orphans()

    assert jumlah == 2
    assert store.get("job-queued")["status"] == "error"
    assert store.get("job-queued")["error_message"] == RESTART_ERROR_MESSAGE
    assert store.get("job-queued")["completed_at"] is not None
    assert store.get("job-done")["status"] == "done"


def test_delete_menghapus_baris(store):
    store.save(_row("job-1"))

    assert store.delete("job-1") is True
    assert store.get("job-1") is None
    assert store.delete("job-1") is False


def test_query_mengurutkan_terbaru_dahulu(store):
    lama = (datetime.now(timezone.utc) - timedelta(hours=2)).isoformat()
    store.save(_row("job-lama", created_at=lama))
    store.save(_row("job-baru"))

    hasil = store.query()

    assert [r["job_id"] for r in hasil] == ["job-baru", "job-lama"]


def test_list_purgeable_files_hanya_berkas_yang_sudah_lewat_batas(store, tmp_path):
    berkas = tmp_path / "hasil.docx"
    berkas.write_bytes(b"isi")
    lama = (datetime.now(timezone.utc) - timedelta(minutes=120)).isoformat()

    store.save(_row("job-lama", completed_at=lama, result_filepath=str(berkas)))
    store.save(_row("job-baru", result_filepath=str(berkas)))

    hasil = store.list_purgeable_files(max_age_minutes=60)

    assert hasil == [("job-lama", str(berkas))]


def test_mark_file_purged_mempertahankan_baris(store):
    store.save(_row("job-1", result_filepath="/tmp/hasil.docx"))

    store.mark_file_purged("job-1")

    baris = store.get("job-1")
    assert baris is not None
    assert baris["file_purged"] == 1
    assert baris["result_filepath"] is None
    assert baris["status"] == "done"


def test_purge_expired_records_menghapus_baris_yang_terlalu_tua(store):
    tua = (datetime.now(timezone.utc) - timedelta(days=40)).isoformat()
    store.save(_row("job-tua", created_at=tua))
    store.save(_row("job-muda"))

    jumlah = store.purge_expired_records(retention_days=30)

    assert jumlah == 1
    assert store.get("job-tua") is None
    assert store.get("job-muda") is not None


def test_list_hydratable_melewati_baris_yang_berkasnya_sudah_dihapus(store):
    store.save(_row("job-utuh"))
    store.save(_row("job-terhapus", file_purged=1))

    hasil = store.list_hydratable()

    assert [r["job_id"] for r in hasil] == ["job-utuh"]


def test_membuat_direktori_induk_bila_belum_ada(tmp_path):
    db_path = str(tmp_path / "belum" / "ada" / "jobs.db")
    s = JobStore(db_path=db_path)
    try:
        assert os.path.exists(db_path)
    finally:
        s.close()
```

- [ ] **Step 2: Jalankan test untuk memastikan gagal**

```bash
cd "C:/Users/ROG/Documents/query db/v2/msf-db/backend" && python -m pytest tests/test_job_store.py -v
```

Expected: FAIL dengan `ModuleNotFoundError: No module named 'app.background.job_store'`.

- [ ] **Step 3: Tulis `job_store.py`**

Buat `v2/msf-db/backend/app/background/job_store.py`:

```python
"""
JobStore — penyimpanan metadata job persisten berbasis SQLite.

Batas tanggung jawab (spec 5.3): JobStore hanya membaca dan menulis baris job.
Ia TIDAK menjalankan job, TIDAK menghapus berkas hasil dari disk, dan TIDAK
mengetahui HTTP. Penghapusan berkas adalah tanggung jawab JobQueue.
"""

import os
import sqlite3
import threading
from datetime import datetime, timedelta, timezone
from typing import Any, Dict, List, Optional, Tuple

SCHEMA_VERSION = 1

RESTART_ERROR_MESSAGE = "Pekerjaan terhenti karena server dimulai ulang."

COLUMNS: Tuple[str, ...] = (
    "job_id",
    "access_code",
    "project_name",
    "status",
    "progress",
    "tables_total",
    "tables_processed",
    "current_table",
    "created_at",
    "updated_at",
    "completed_at",
    "error_message",
    "preview_markdown",
    "result_filepath",
    "result_filename",
    "output_format",
    "ai_provider",
    "db_engine",
    "file_purged",
)

_SCHEMA_SQL = """
PRAGMA journal_mode = WAL;

CREATE TABLE IF NOT EXISTS jobs (
    job_id           TEXT PRIMARY KEY,
    access_code      TEXT NOT NULL,
    project_name     TEXT NOT NULL,
    status           TEXT NOT NULL,
    progress         INTEGER NOT NULL DEFAULT 0,
    tables_total     INTEGER NOT NULL DEFAULT 0,
    tables_processed INTEGER NOT NULL DEFAULT 0,
    current_table    TEXT,
    created_at       TEXT NOT NULL,
    updated_at       TEXT NOT NULL,
    completed_at     TEXT,
    error_message    TEXT,
    preview_markdown TEXT,
    result_filepath  TEXT,
    result_filename  TEXT,
    output_format    TEXT NOT NULL DEFAULT 'docx',
    ai_provider      TEXT,
    db_engine        TEXT,
    file_purged      INTEGER NOT NULL DEFAULT 0
);

CREATE INDEX IF NOT EXISTS idx_jobs_access_code ON jobs(access_code);
CREATE INDEX IF NOT EXISTS idx_jobs_created_at  ON jobs(created_at);
CREATE INDEX IF NOT EXISTS idx_jobs_status      ON jobs(status);
"""


class JobStore:
    """Penyimpanan baris job. Aman dipakai lintas thread lewat satu lock."""

    def __init__(self, db_path: Optional[str] = None) -> None:
        self.db_path = db_path or os.getenv("JOBS_DB_PATH", "/app/outputs/jobs.db")
        directory = os.path.dirname(self.db_path)
        if directory:
            os.makedirs(directory, exist_ok=True)

        self._lock = threading.Lock()
        self._conn = sqlite3.connect(self.db_path, check_same_thread=False)
        self._conn.row_factory = sqlite3.Row
        self._init_schema()

    def _init_schema(self) -> None:
        with self._lock:
            self._conn.executescript(_SCHEMA_SQL)
            current = self._conn.execute("PRAGMA user_version").fetchone()[0]
            if current > SCHEMA_VERSION:
                raise RuntimeError(
                    f"Basis data job memakai skema versi {current}, "
                    f"sedangkan kode ini hanya mendukung versi {SCHEMA_VERSION}."
                )
            self._conn.execute(f"PRAGMA user_version = {SCHEMA_VERSION}")
            self._conn.commit()

    def save(self, row: Dict[str, Any]) -> None:
        """Simpan atau timpa satu baris job."""
        kolom = ", ".join(COLUMNS)
        placeholder = ", ".join(f":{c}" for c in COLUMNS)
        data = {c: row.get(c) for c in COLUMNS}
        with self._lock:
            self._conn.execute(
                f"INSERT OR REPLACE INTO jobs ({kolom}) VALUES ({placeholder})", data
            )
            self._conn.commit()

    def get(self, job_id: str) -> Optional[Dict[str, Any]]:
        with self._lock:
            row = self._conn.execute(
                "SELECT * FROM jobs WHERE job_id = :job_id", {"job_id": job_id}
            ).fetchone()
        return dict(row) if row else None

    def get_by_access_code(self, access_code: str) -> Optional[Dict[str, Any]]:
        with self._lock:
            row = self._conn.execute(
                "SELECT * FROM jobs WHERE access_code = :code ORDER BY created_at DESC LIMIT 1",
                {"code": access_code},
            ).fetchone()
        return dict(row) if row else None

    def query(self, limit: int = 500) -> List[Dict[str, Any]]:
        """Seluruh baris, terbaru dahulu. Dipakai Admin Portal."""
        with self._lock:
            rows = self._conn.execute(
                "SELECT * FROM jobs ORDER BY created_at DESC LIMIT :limit",
                {"limit": limit},
            ).fetchall()
        return [dict(r) for r in rows]

    def list_hydratable(self) -> List[Dict[str, Any]]:
        """Baris yang layak dimuat kembali ke memori saat startup."""
        with self._lock:
            rows = self._conn.execute(
                "SELECT * FROM jobs WHERE file_purged = 0 ORDER BY created_at DESC"
            ).fetchall()
        return [dict(r) for r in rows]

    def delete(self, job_id: str) -> bool:
        with self._lock:
            cur = self._conn.execute(
                "DELETE FROM jobs WHERE job_id = :job_id", {"job_id": job_id}
            )
            self._conn.commit()
            return cur.rowcount > 0

    def reconcile_orphans(self) -> int:
        """
        Tandai job yang masih queued/processing sebagai error.

        Wajib dijalankan saat startup sebelum melayani request: tanpa ini job
        yatim mengisi gerbang MAX_CONCURRENT_JOBS secara permanen (spec 9.1).
        """
        now = datetime.now(timezone.utc).isoformat()
        with self._lock:
            cur = self._conn.execute(
                """
                UPDATE jobs
                   SET status = 'error',
                       error_message = :message,
                       completed_at = :now,
                       updated_at = :now
                 WHERE status IN ('queued', 'processing')
                """,
                {"message": RESTART_ERROR_MESSAGE, "now": now},
            )
            self._conn.commit()
            return cur.rowcount

    def list_purgeable_files(self, max_age_minutes: int) -> List[Tuple[str, str]]:
        """Pasangan (job_id, filepath) untuk berkas yang sudah melewati retensi."""
        cutoff = (
            datetime.now(timezone.utc) - timedelta(minutes=max_age_minutes)
        ).isoformat()
        with self._lock:
            rows = self._conn.execute(
                """
                SELECT job_id, result_filepath FROM jobs
                 WHERE file_purged = 0
                   AND result_filepath IS NOT NULL
                   AND completed_at IS NOT NULL
                   AND completed_at < :cutoff
                 ORDER BY completed_at ASC
                """,
                {"cutoff": cutoff},
            ).fetchall()
        return [(r["job_id"], r["result_filepath"]) for r in rows]

    def mark_file_purged(self, job_id: str) -> None:
        """Tandai berkas sudah dihapus. Baris riwayatnya sengaja dipertahankan."""
        now = datetime.now(timezone.utc).isoformat()
        with self._lock:
            self._conn.execute(
                """
                UPDATE jobs
                   SET file_purged = 1,
                       result_filepath = NULL,
                       updated_at = :now
                 WHERE job_id = :job_id
                """,
                {"job_id": job_id, "now": now},
            )
            self._conn.commit()

    def purge_expired_records(self, retention_days: int) -> int:
        cutoff = (
            datetime.now(timezone.utc) - timedelta(days=retention_days)
        ).isoformat()
        with self._lock:
            cur = self._conn.execute(
                "DELETE FROM jobs WHERE created_at < :cutoff", {"cutoff": cutoff}
            )
            self._conn.commit()
            return cur.rowcount

    def close(self) -> None:
        with self._lock:
            self._conn.close()
```

- [ ] **Step 4: Jalankan test JobStore untuk memastikan lulus**

```bash
cd "C:/Users/ROG/Documents/query db/v2/msf-db/backend" && python -m pytest tests/test_job_store.py -v
```

Expected: 13 PASS.

- [ ] **Step 5: Tulis test JobQueue yang gagal**

Tambahkan di `v2/msf-db/backend/tests/test_job_queue.py`:

```python
def test_create_job_langsung_tersimpan_ke_store(isolated_job_queue):
    job = isolated_job_queue.create_job(project_name="Proyek Persisten")

    baris = isolated_job_queue.store.get(job.job_id)
    assert baris is not None
    assert baris["project_name"] == "Proyek Persisten"
    assert baris["status"] == "queued"


def test_update_job_mempersist_perubahan(isolated_job_queue):
    job = isolated_job_queue.create_job()

    job.update(progress=45, current_table="pengguna")

    baris = isolated_job_queue.store.get(job.job_id)
    assert baris["progress"] == 45
    assert baris["current_table"] == "pengguna"


def test_get_job_jatuh_ke_store_saat_memori_kosong(isolated_job_queue):
    job = isolated_job_queue.create_job()
    job_id = job.job_id
    isolated_job_queue._jobs.clear()

    hasil = isolated_job_queue.get_job(job_id)

    assert hasil is not None
    assert hasil.job_id == job_id


def test_get_job_by_access_code_jatuh_ke_store(isolated_job_queue):
    job = isolated_job_queue.create_job()
    kode = job.access_code
    isolated_job_queue._jobs.clear()

    hasil = isolated_job_queue.get_job_by_access_code(kode)

    assert hasil is not None
    assert hasil.access_code == kode


def test_job_dari_store_adalah_snapshot_baca_saja(isolated_job_queue):
    job = isolated_job_queue.create_job()
    job_id = job.job_id
    isolated_job_queue._jobs.clear()

    snapshot = isolated_job_queue.get_job(job_id)
    snapshot.update(progress=99)

    assert isolated_job_queue.store.get(job_id)["progress"] == 0


def test_recover_menandai_job_aktif_sebagai_error(isolated_job_queue):
    from app.background.job_store import RESTART_ERROR_MESSAGE

    job = isolated_job_queue.create_job()
    job_id = job.job_id
    isolated_job_queue._jobs.clear()

    jumlah = isolated_job_queue.recover()

    assert jumlah == 1
    pulih = isolated_job_queue.get_job(job_id)
    assert pulih.status == "error"
    assert pulih.error_message == RESTART_ERROR_MESSAGE


def test_recover_tidak_menyisakan_job_aktif_di_gerbang_konkurensi(isolated_job_queue):
    for _ in range(3):
        isolated_job_queue.create_job()
    isolated_job_queue._jobs.clear()

    isolated_job_queue.recover()

    aktif = [
        j for j in isolated_job_queue.list_jobs()
        if j["status"] in ("queued", "processing")
    ]
    assert aktif == []


def test_delete_job_menghapus_baris_di_store(isolated_job_queue):
    job = isolated_job_queue.create_job()

    assert isolated_job_queue.delete_job(job.job_id) is True
    assert isolated_job_queue.store.get(job.job_id) is None
```

- [ ] **Step 6: Jalankan test untuk memastikan gagal**

```bash
cd "C:/Users/ROG/Documents/query db/v2/msf-db/backend" && python -m pytest tests/test_job_queue.py -v
```

Expected: FAIL dengan `AttributeError: 'JobQueue' object has no attribute 'store'`.

- [ ] **Step 7: Sambungkan `Job` dan `JobQueue` ke `JobStore`**

Pada `v2/msf-db/backend/app/background/job_queue.py`:

Tambahkan import di bagian atas berkas:

```python
from app.background.job_store import JobStore
```

Ganti `Job.__init__` menjadi (tambahan: parameter `on_change` dan atribut `file_purged`):

```python
class Job:
    def __init__(
        self,
        job_id: str,
        project_name: str = "Database Documentation",
        on_change: Optional[Callable[["Job"], None]] = None,
    ):
        self.job_id = job_id
        self.project_name = project_name
        self.status: JobStatus = JobStatus.QUEUED
        self.progress: int = 0
        self.tables_total: int = 0
        self.tables_processed: int = 0
        self.current_table: Optional[str] = None
        self.created_at: str = datetime.now(timezone.utc).isoformat()
        self.updated_at: str = datetime.now(timezone.utc).isoformat()
        self.completed_at: Optional[str] = None
        self.error_message: Optional[str] = None
        self.preview_markdown: Optional[str] = None
        self.result_filepath: Optional[str] = None       # Path file DOCX/PDF di disk
        self.result_filename: Optional[str] = None       # Nama file untuk download
        self.output_format: str = "docx"
        self.ai_provider: Optional[str] = None           # Tracking untuk statistik admin
        self.db_engine: Optional[str] = None             # Tracking untuk statistik admin
        self.access_code: str = f"MSF-{secrets.token_hex(5).upper()}" # Kode pelacakan unik untuk user
        self.file_purged: bool = False                   # True bila berkas sudah dihapus penyapu
        self._on_change = on_change
```

Tambahkan dua metode pada `Job`, tepat setelah `to_dict()`:

```python
    def to_row(self) -> dict:
        """Bentuk baris untuk JobStore. Hanya metadata, tidak pernah isi berkas."""
        status = self.status.value if hasattr(self.status, "value") else str(self.status)
        return {
            "job_id": self.job_id,
            "access_code": self.access_code,
            "project_name": self.project_name,
            "status": status,
            "progress": self.progress,
            "tables_total": self.tables_total,
            "tables_processed": self.tables_processed,
            "current_table": self.current_table,
            "created_at": self.created_at,
            "updated_at": self.updated_at,
            "completed_at": self.completed_at,
            "error_message": self.error_message,
            "preview_markdown": self.preview_markdown,
            "result_filepath": self.result_filepath,
            "result_filename": self.result_filename,
            "output_format": self.output_format,
            "ai_provider": self.ai_provider,
            "db_engine": self.db_engine,
            "file_purged": 1 if self.file_purged else 0,
        }

    @classmethod
    def from_row(cls, row: dict) -> "Job":
        """
        Bangun Job dari baris basis data sebagai snapshot BACA-SAJA.

        on_change sengaja dibiarkan None: job hasil hidrasi tidak boleh menulis
        balik ke store (invarian spec 11.2).
        """
        job = cls(job_id=row["job_id"], project_name=row["project_name"])
        job.status = row["status"]
        job.progress = row["progress"]
        job.tables_total = row["tables_total"]
        job.tables_processed = row["tables_processed"]
        job.current_table = row["current_table"]
        job.created_at = row["created_at"]
        job.updated_at = row["updated_at"]
        job.completed_at = row["completed_at"]
        job.error_message = row["error_message"]
        job.preview_markdown = row["preview_markdown"]
        job.result_filepath = row["result_filepath"]
        job.result_filename = row["result_filename"]
        job.output_format = row["output_format"]
        job.ai_provider = row["ai_provider"]
        job.db_engine = row["db_engine"]
        job.access_code = row["access_code"]
        job.file_purged = bool(row["file_purged"])
        return job
```

Pada `Job.update()`, ganti dua baris terakhir menjadi:

```python
        self.updated_at = datetime.now(timezone.utc).isoformat()
        if self._on_change is not None:
            self._on_change(self)
```

Ganti `JobQueue.__init__`, `create_job`, `get_job`, `get_job_by_access_code`, dan `delete_job`:

```python
    def __init__(
        self,
        max_retention_minutes: Optional[int] = None,
        store: Optional[JobStore] = None,
    ):
        self._jobs: Dict[str, Job] = {}
        self._max_retention_minutes = (
            max_retention_minutes
            if max_retention_minutes is not None
            else int(os.getenv("MAX_JOB_RETENTION_MINUTES", "60"))
        )
        self._record_retention_days = int(os.getenv("JOB_RECORD_RETENTION_DAYS", "30"))
        self._lock = asyncio.Lock()
        self._store = store if store is not None else JobStore()

    @property
    def store(self) -> JobStore:
        return self._store

    def _persist(self, job: Job) -> None:
        self._store.save(job.to_row())

    def recover(self) -> int:
        """
        Pulihkan keadaan dari basis data saat startup.

        Job yang tertinggal queued/processing ditandai error, lalu job yang
        berkasnya masih ada dimuat kembali ke memori sebagai snapshot baca-saja.
        Mengembalikan jumlah job yatim yang ditandai error.
        """
        orphans = self._store.reconcile_orphans()
        for row in self._store.list_hydratable():
            job = Job.from_row(row)
            self._jobs[job.job_id] = job
        return orphans

    def create_job(self, project_name: str = "Database Documentation") -> Job:
        """Buat job baru, simpan baris awalnya, dan return job object"""
        job_id = str(uuid.uuid4())
        job = Job(job_id=job_id, project_name=project_name, on_change=self._persist)
        self._jobs[job_id] = job
        self._persist(job)
        return job

    def get_job(self, job_id: str) -> Optional[Job]:
        """Ambil job dari memori; bila tidak ada, cari di basis data"""
        job = self._jobs.get(job_id)
        if job is not None:
            return job
        row = self._store.get(job_id)
        return Job.from_row(row) if row else None

    def get_job_by_access_code(self, access_code: str) -> Optional[Job]:
        """Ambil job berdasarkan kode akses; memori dulu, lalu basis data"""
        for job in self._jobs.values():
            if job.access_code == access_code:
                return job
        row = self._store.get_by_access_code(access_code)
        return Job.from_row(row) if row else None

    def delete_job(self, job_id: str) -> bool:
        """Hapus job dari memori dan basis data, berikut filenya"""
        job = self._jobs.pop(job_id, None)
        if job is not None:
            job.cleanup_file()
        return self._store.delete(job_id) or job is not None

    def close(self) -> None:
        self._store.close()
```

Terakhir, ganti baris singleton di akhir berkas:

```python
# Singleton instance — dipakai di seluruh app
job_queue = JobQueue()
```

- [ ] **Step 8: Perbarui fixture agar memakai SQLite sementara**

Pertama, beri nilai bawaan yang aman di TINGKAT MODUL `v2/msf-db/backend/conftest.py`, tepat setelah blok import. Ini wajib: singleton `job_queue` dibangun saat modul di-import, yaitu sebelum fixture mana pun berjalan, dan path bawaan `/app/outputs/jobs.db` tidak valid di luar container.

```python
import os
import tempfile

# Dijalankan saat conftest di-import, sebelum modul test mana pun dikumpulkan.
os.environ.setdefault(
    "JOBS_DB_PATH", os.path.join(tempfile.gettempdir(), "msf_test_jobs.db")
)
os.environ.setdefault("OUTPUT_DIR", tempfile.gettempdir())
```

Lalu di dalam `isolated_job_queue`, tambahkan sebelum pembuatan `JobQueue`:

```python
    monkeypatch.setenv("JOBS_DB_PATH", str(tmp_path / "jobs.db"))
```

lalu ganti baris pembuatan dan penutupan:

```python
    fresh = jq_module.JobQueue()
```

dan ganti `yield fresh` di akhir fixture menjadi:

```python
    yield fresh

    fresh.close()
```

- [ ] **Step 9: Jalankan test JobQueue untuk memastikan lulus**

```bash
cd "C:/Users/ROG/Documents/query db/v2/msf-db/backend" && python -m pytest tests/test_job_queue.py tests/test_job_store.py -v
```

Expected: PASS seluruhnya.

- [ ] **Step 10: Jalankan rekonsiliasi di lifespan**

Pada `v2/msf-db/backend/app/main.py`, di dalam `lifespan`, tepat setelah baris `logger.info("MSF-APP Backend starting up...")` dan SEBELUM `cleanup_task` dibuat:

```python
    # Rekonsiliasi job yatim WAJIB selesai sebelum yield: tanpa ini job yang mati
    # saat restart tetap terhitung queued/processing dan mengunci gerbang
    # MAX_CONCURRENT_JOBS secara permanen (spec 9.1).
    try:
        orphans = job_queue.recover()
        if orphans > 0:
            logger.warning(
                "Job yatim ditandai error setelah restart", jumlah=orphans
            )
    except Exception as e:
        logger.error("Gagal memulihkan antrean job dari basis data", error=str(e))
```

Tambahkan penutupan store setelah `cleanup_task.cancel()`:

```python
    try:
        job_queue.close()
    except Exception as e:
        logger.error("Gagal menutup basis data job", error=str(e))
```

- [ ] **Step 11: Jalankan seluruh suite**

```bash
cd "C:/Users/ROG/Documents/query db/v2/msf-db/backend" && python -m pytest -q
```

Expected: PASS seluruhnya.

- [ ] **Step 12: Commit**

```bash
cd "C:/Users/ROG/Documents/query db" && git add v2/msf-db/backend && git commit -m "feat: tambah JobStore SQLite dan rekonsiliasi job yatim"
```

---

## Task 6: Retensi dua tingkat, 410 RESULT_EXPIRED, dan statistik admin lintas 30 hari

Spec 11.2 (bagian pembersihan), 9.2, 9.3, 7.3, D2, D3.

**Files:**
- Modify: `v2/msf-db/backend/app/background/job_queue.py` (ganti `cleanup_old_jobs`)
- Modify: `v2/msf-db/backend/app/main.py:78-88` (loop pembersihan)
- Modify: `v2/msf-db/backend/app/routers/generate.py:293-328` (download)
- Modify: `v2/msf-db/backend/app/routers/admin.py:45-108` (sumber statistik)
- Test: `v2/msf-db/backend/tests/test_job_retention.py` (baru)
- Test: `v2/msf-db/backend/tests/test_download_expired.py` (baru)
- Test: `v2/msf-db/backend/tests/test_admin.py` (diperluas)

**Interfaces:**
- Consumes: `JobStore.list_purgeable_files`, `JobStore.mark_file_purged`, `JobStore.purge_expired_records`, `JobStore.query`, `Job.file_purged` dari Task 5.
- Produces: `JobQueue.purge_expired_files() -> int` (async), `JobQueue.purge_expired_records() -> int` (async). Menggantikan `JobQueue.cleanup_old_jobs()` yang dihapus.

- [ ] **Step 1: Tulis test retensi yang gagal**

Buat `v2/msf-db/backend/tests/test_job_retention.py`:

```python
"""Retensi berkas (menit) terpisah dari retensi baris riwayat (hari)."""

import os
from datetime import datetime, timedelta, timezone

import pytest


@pytest.mark.asyncio
async def test_purge_expired_files_menghapus_berkas_tapi_mempertahankan_baris(
    isolated_job_queue, tmp_path
):
    berkas = tmp_path / "hasil.docx"
    berkas.write_bytes(b"isi dokumen")

    job = isolated_job_queue.create_job()
    lama = (datetime.now(timezone.utc) - timedelta(minutes=120)).isoformat()
    job.update(
        status="done",
        completed_at=lama,
        result_filepath=str(berkas),
    )

    jumlah = await isolated_job_queue.purge_expired_files()

    assert jumlah == 1
    assert not os.path.exists(str(berkas))

    baris = isolated_job_queue.store.get(job.job_id)
    assert baris is not None
    assert baris["file_purged"] == 1
    assert baris["result_filepath"] is None


@pytest.mark.asyncio
async def test_purge_expired_files_tidak_menyentuh_berkas_yang_masih_muda(
    isolated_job_queue, tmp_path
):
    berkas = tmp_path / "hasil.docx"
    berkas.write_bytes(b"isi dokumen")

    job = isolated_job_queue.create_job()
    job.update(
        status="done",
        completed_at=datetime.now(timezone.utc).isoformat(),
        result_filepath=str(berkas),
    )

    jumlah = await isolated_job_queue.purge_expired_files()

    assert jumlah == 0
    assert os.path.exists(str(berkas))


@pytest.mark.asyncio
async def test_purge_expired_records_menghapus_baris_lebih_tua_dari_retensi(
    isolated_job_queue,
):
    job = isolated_job_queue.create_job()
    tua = (datetime.now(timezone.utc) - timedelta(days=40)).isoformat()
    job.update(status="done", created_at=tua, completed_at=tua)

    jumlah = await isolated_job_queue.purge_expired_records()

    assert jumlah == 1
    assert isolated_job_queue.store.get(job.job_id) is None


def test_max_job_retention_minutes_dibaca_dari_env(monkeypatch, tmp_path):
    """MAX_JOB_RETENTION_MINUTES sebelumnya config mati (spec 9.3)."""
    import app.background.job_queue as jq_module

    monkeypatch.setenv("JOBS_DB_PATH", str(tmp_path / "jobs-env.db"))
    monkeypatch.setenv("MAX_JOB_RETENTION_MINUTES", "5")
    monkeypatch.setenv("JOB_RECORD_RETENTION_DAYS", "7")

    antrean = jq_module.JobQueue()
    try:
        assert antrean._max_retention_minutes == 5
        assert antrean._record_retention_days == 7
    finally:
        antrean.close()
```

- [ ] **Step 2: Jalankan test untuk memastikan gagal**

```bash
cd "C:/Users/ROG/Documents/query db/v2/msf-db/backend" && python -m pytest tests/test_job_retention.py -v
```

Expected: FAIL dengan `AttributeError: 'JobQueue' object has no attribute 'purge_expired_files'`.

- [ ] **Step 3: Ganti `cleanup_old_jobs` dengan dua pembersihan**

Pada `v2/msf-db/backend/app/background/job_queue.py`, HAPUS seluruh metode `cleanup_old_jobs` dan gantikan dengan:

```python
    async def purge_expired_files(self) -> int:
        """
        Hapus berkas hasil yang sudah melewati MAX_JOB_RETENTION_MINUTES.

        Baris riwayatnya sengaja DIPERTAHANKAN dan ditandai file_purged agar
        kode akses tetap dapat dilacak sampai retensi riwayat habis (spec D2).
        """
        jumlah = 0
        for job_id, filepath in self._store.list_purgeable_files(
            self._max_retention_minutes
        ):
            if filepath and os.path.exists(filepath):
                try:
                    os.remove(filepath)
                except OSError as e:
                    import structlog
                    structlog.get_logger().warning(
                        "Gagal menghapus berkas hasil kedaluwarsa",
                        job_id=job_id,
                        error=str(e),
                    )
                    continue
            self._store.mark_file_purged(job_id)
            self._jobs.pop(job_id, None)
            jumlah += 1
        return jumlah

    async def purge_expired_records(self) -> int:
        """Hapus baris riwayat yang lebih tua dari JOB_RECORD_RETENTION_DAYS."""
        return self._store.purge_expired_records(self._record_retention_days)
```

- [ ] **Step 4: Panggil kedua pembersihan dari loop di `main.py`**

Pada `v2/msf-db/backend/app/main.py`, ganti isi `cleanup_loop`:

```python
    # Background task: dua pembersihan terpisah setiap 15 menit
    async def cleanup_loop():
        while True:
            try:
                await asyncio.sleep(15 * 60)
                berkas = await job_queue.purge_expired_files()
                baris = await job_queue.purge_expired_records()
                if berkas > 0 or baris > 0:
                    logger.info(
                        "Pembersihan terjadwal selesai",
                        berkas_dihapus=berkas,
                        baris_dihapus=baris,
                    )
            except Exception as e:
                logger.error("Error in job cleanup loop", error=str(e))
```

- [ ] **Step 5: Jalankan test retensi untuk memastikan lulus**

```bash
cd "C:/Users/ROG/Documents/query db/v2/msf-db/backend" && python -m pytest tests/test_job_retention.py -v
```

Expected: 4 PASS.

- [ ] **Step 6: Tulis test download kedaluwarsa yang gagal**

Buat `v2/msf-db/backend/tests/test_download_expired.py`:

```python
"""Berkas hasil yang sudah dihapus penyapu harus menjawab 410, bukan 500."""

from datetime import datetime, timezone

from fastapi.testclient import TestClient


def test_download_berkas_kedaluwarsa_menjawab_410(isolated_job_queue):
    from app.main import app

    job = isolated_job_queue.create_job()
    job.update(
        status="done",
        progress=100,
        completed_at=datetime.now(timezone.utc).isoformat(),
        result_filename="dokumentasi.docx",
    )
    isolated_job_queue.store.mark_file_purged(job.job_id)
    isolated_job_queue._jobs.clear()

    with TestClient(app) as c:
        response = c.get(f"/api/jobs/{job.job_id}/download")

    assert response.status_code == 410
    body = response.json()
    assert body["error_code"] == "RESULT_EXPIRED"
    assert "timestamp" in body


def test_download_job_tidak_dikenal_tetap_404(isolated_job_queue):
    from app.main import app

    with TestClient(app) as c:
        response = c.get("/api/jobs/job-tidak-ada/download")

    assert response.status_code == 404
```

- [ ] **Step 7: Jalankan test untuk memastikan gagal**

```bash
cd "C:/Users/ROG/Documents/query db/v2/msf-db/backend" && python -m pytest tests/test_download_expired.py -v
```

Expected: `test_download_berkas_kedaluwarsa_menjawab_410` FAIL dengan status 500.

- [ ] **Step 8: Tambahkan cabang 410 di endpoint download**

Pada `v2/msf-db/backend/app/routers/generate.py`, tambahkan import di bagian atas berkas:

```python
from app.utils.errors import AppDetailedException
```

lalu di dalam `download_job_result`, sisipkan sebelum pemeriksaan `result_filepath` yang sudah ada:

```python
    if job.file_purged:
        raise AppDetailedException(
            detail=(
                "Berkas hasil sudah kedaluwarsa dan dihapus dari server. "
                "Silakan jalankan ulang proses generate."
            ),
            status_code=410,
            error_code="RESULT_EXPIRED",
        )
```

- [ ] **Step 9: Jalankan test download untuk memastikan lulus**

```bash
cd "C:/Users/ROG/Documents/query db/v2/msf-db/backend" && python -m pytest tests/test_download_expired.py -v
```

Expected: 2 PASS.

- [ ] **Step 10: Tulis test statistik admin yang gagal**

Tambahkan di `v2/msf-db/backend/tests/test_admin.py`:

```python
def test_stats_bersumber_dari_riwayat_penuh(isolated_job_queue, monkeypatch):
    """Statistik admin harus melintasi riwayat basis data, bukan hanya memori."""
    import app.routers.admin as admin_module
    from datetime import datetime, timezone
    from fastapi.testclient import TestClient
    from app.main import app

    monkeypatch.setattr(admin_module, "ADMIN_PASSCODE", "passcode-benar")

    job = isolated_job_queue.create_job(project_name="Riwayat Lama")
    job.update(status="done", progress=100, completed_at=datetime.now(timezone.utc).isoformat())
    isolated_job_queue._jobs.clear()

    with TestClient(app) as c:
        response = c.get("/api/admin/stats", headers={"X-Admin-Passcode": "passcode-benar"})

    assert response.status_code == 200
    assert response.json()["summary"]["total_jobs"] == 1


def test_daftar_jobs_admin_bersumber_dari_riwayat_penuh(isolated_job_queue, monkeypatch):
    import app.routers.admin as admin_module
    from fastapi.testclient import TestClient
    from app.main import app

    monkeypatch.setattr(admin_module, "ADMIN_PASSCODE", "passcode-benar")

    isolated_job_queue.create_job(project_name="Riwayat Lama")
    isolated_job_queue._jobs.clear()

    with TestClient(app) as c:
        response = c.get("/api/admin/jobs", headers={"X-Admin-Passcode": "passcode-benar"})

    assert response.status_code == 200
    assert len(response.json()["jobs"]) == 1
```

- [ ] **Step 11: Jalankan test untuk memastikan gagal**

```bash
cd "C:/Users/ROG/Documents/query db/v2/msf-db/backend" && python -m pytest tests/test_admin.py -v
```

Expected: kedua test baru FAIL karena `total_jobs` dan panjang daftar bernilai 0.

- [ ] **Step 12: Alihkan sumber data admin ke `JobStore.query()`**

Pada `v2/msf-db/backend/app/routers/admin.py`:

Di `get_admin_stats`, ganti baris `jobs = job_queue.list_jobs()` menjadi:

```python
    # Sumbernya riwayat penuh di basis data, bukan memori. Sejak v2.2.0 angka ini
    # melintasi JOB_RECORD_RETENTION_DAYS, bukan lagi 60 menit terakhir (spec D3).
    jobs = job_queue.store.query()
```

Di `get_all_jobs`, ganti baris yang sama menjadi:

```python
    jobs = job_queue.store.query()
```

Di `force_cleanup_system`, ganti baris `jobs = job_queue.list_jobs()` menjadi:

```python
        # Pembatalan hanya berlaku untuk job yang benar-benar berjalan (memori),
        # sedangkan penghapusan mencakup seluruh riwayat di basis data.
        aktif = job_queue.list_jobs()
        active_ids = [
            j["job_id"] for j in aktif
            if j["status"] in (JobStatus.QUEUED, JobStatus.PROCESSING)
        ]
        for job_id in active_ids:
            await job_queue.cancel_job(job_id)

        jobs = job_queue.store.query()
```

dan pastikan baris berikutnya tetap `all_ids = [j["job_id"] for j in jobs]` diikuti loop `job_queue.delete_job(job_id)`. Hapus blok pembatalan lama agar tidak ganda.

- [ ] **Step 13: Jalankan seluruh suite**

```bash
cd "C:/Users/ROG/Documents/query db/v2/msf-db/backend" && python -m pytest -q
```

Expected: PASS seluruhnya.

- [ ] **Step 14: Commit**

```bash
cd "C:/Users/ROG/Documents/query db" && git add v2/msf-db/backend && git commit -m "feat: pisahkan retensi berkas dan retensi riwayat job"
```

---

## Task 7: Rate limiting per IP

Spec 11.3, 9.5, 9.6, 7.3, 8.2, D9, NFR-S1.

**Files:**
- Create: `v2/msf-db/backend/app/utils/rate_limit.py`
- Modify: `v2/msf-db/backend/requirements.txt`
- Modify: `v2/msf-db/backend/app/main.py` (registrasi limiter dan handler)
- Modify: `v2/msf-db/backend/app/routers/generate.py` (dekorator + `error_code` 429)
- Modify: `v2/msf-db/backend/app/routers/admin.py` (dekorator pada `/verify`)
- Test: `v2/msf-db/backend/tests/test_rate_limit.py` (baru)

**Interfaces:**
- Consumes: `AppDetailedException` (sudah dipakai Task 6).
- Produces: `limiter: Limiter`, `client_key(request) -> str`, `generate_limit() -> str`, `admin_verify_limit() -> str`, `rate_limit_enabled() -> bool`, `rate_limit_exceeded_handler(request, exc) -> JSONResponse`. Dipakai Task 8 lewat `error_code` pada respons.

- [ ] **Step 1: Pasang dependensi**

Pada `v2/msf-db/backend/requirements.txt`, tambahkan di bagian UTILITIES:

```
slowapi==0.1.9                  # Rate limiting per IP
```

lalu pasang:

```bash
cd "C:/Users/ROG/Documents/query db/v2/msf-db/backend" && pip install slowapi==0.1.9
```

- [ ] **Step 2: Tulis test yang gagal**

Buat `v2/msf-db/backend/tests/test_rate_limit.py`:

```python
"""Rate limiting per IP dan pembedaan dua jenis 429."""

import pytest
from fastapi.testclient import TestClient


@pytest.fixture
def ddl_payload():
    return {
        "sql_content": "CREATE TABLE pengguna (id INT PRIMARY KEY);",
        "ai_provider": "ollama",
        "model": "llama3.2",
        "language": "Indonesian",
        "detail_level": "simple",
        "business_context": "",
        "project_name": "Uji Rate Limit",
        "author": "Developer",
        "output_format": "docx",
    }


def test_client_key_mengabaikan_forwarded_for_secara_bawaan(monkeypatch):
    from starlette.requests import Request
    from app.utils.rate_limit import client_key

    monkeypatch.delenv("RATE_LIMIT_TRUST_FORWARDED_FOR", raising=False)
    scope = {
        "type": "http",
        "headers": [(b"x-forwarded-for", b"203.0.113.9")],
        "client": ("10.0.0.5", 12345),
    }

    assert client_key(Request(scope)) == "10.0.0.5"


def test_client_key_memakai_forwarded_for_saat_flag_dinyalakan(monkeypatch):
    from starlette.requests import Request
    from app.utils.rate_limit import client_key

    monkeypatch.setenv("RATE_LIMIT_TRUST_FORWARDED_FOR", "true")
    scope = {
        "type": "http",
        "headers": [(b"x-forwarded-for", b"203.0.113.9, 10.0.0.1")],
        "client": ("10.0.0.5", 12345),
    }

    assert client_key(Request(scope)) == "203.0.113.9"


def test_rate_limit_enabled_dibaca_dari_env(monkeypatch):
    from app.utils.rate_limit import rate_limit_enabled

    monkeypatch.setenv("RATE_LIMIT_ENABLED", "false")
    assert rate_limit_enabled() is False

    monkeypatch.setenv("RATE_LIMIT_ENABLED", "true")
    assert rate_limit_enabled() is True


def _reset_limiter(rl) -> None:
    """Kosongkan penghitung limit antar test."""
    reset = getattr(rl.limiter, "reset", None)
    if callable(reset):
        reset()
        return
    rl.limiter._storage.reset()


def test_dua_jenis_429_dibedakan_lewat_error_code(monkeypatch, ddl_payload):
    """
    MAX_CONCURRENT_JOBS=0 membuat gerbang antrean selalu menolak, sehingga tidak
    ada job maupun background task yang benar-benar dibuat. Dekorator limiter
    berjalan sebelum badan endpoint, jadi kedua request pertama tetap terhitung.
    """
    from app.main import app
    from app.utils import rate_limit as rl

    monkeypatch.setenv("MAX_CONCURRENT_JOBS", "0")
    monkeypatch.setenv("RATE_LIMIT_GENERATE", "2/minute")
    rl.limiter.enabled = True
    _reset_limiter(rl)

    with TestClient(app) as c:
        pertama = c.post("/api/generate/from-ddl", json=ddl_payload)
        kedua = c.post("/api/generate/from-ddl", json=ddl_payload)
        ketiga = c.post("/api/generate/from-ddl", json=ddl_payload)

    assert pertama.status_code == 429
    assert pertama.json()["error_code"] == "JOB_QUEUE_FULL"
    assert kedua.json()["error_code"] == "JOB_QUEUE_FULL"

    assert ketiga.status_code == 429
    body = ketiga.json()
    assert body["error_code"] == "RATE_LIMIT_EXCEEDED"
    assert "timestamp" in body
    assert int(ketiga.headers["Retry-After"]) > 0


def test_rate_limit_dapat_dimatikan(monkeypatch, ddl_payload):
    from app.main import app
    from app.utils import rate_limit as rl

    monkeypatch.setenv("MAX_CONCURRENT_JOBS", "0")
    monkeypatch.setenv("RATE_LIMIT_GENERATE", "1/minute")
    rl.limiter.enabled = False
    _reset_limiter(rl)

    try:
        with TestClient(app) as c:
            for _ in range(3):
                response = c.post("/api/generate/from-ddl", json=ddl_payload)
                assert response.json()["error_code"] == "JOB_QUEUE_FULL"
    finally:
        rl.limiter.enabled = True


def test_antrean_penuh_menjawab_429_job_queue_full(
    monkeypatch, isolated_job_queue, ddl_payload
):
    from app.main import app
    from app.utils import rate_limit as rl

    rl.limiter.enabled = False
    monkeypatch.setenv("MAX_CONCURRENT_JOBS", "1")
    isolated_job_queue.create_job(project_name="Job aktif")

    try:
        with TestClient(app) as c:
            response = c.post("/api/generate/from-ddl", json=ddl_payload)
    finally:
        rl.limiter.enabled = True

    assert response.status_code == 429
    assert response.json()["error_code"] == "JOB_QUEUE_FULL"
```

- [ ] **Step 3: Jalankan test untuk memastikan gagal**

```bash
cd "C:/Users/ROG/Documents/query db/v2/msf-db/backend" && python -m pytest tests/test_rate_limit.py -v
```

Expected: FAIL dengan `ModuleNotFoundError: No module named 'app.utils.rate_limit'`.

- [ ] **Step 4: Tulis `rate_limit.py`**

Buat `v2/msf-db/backend/app/utils/rate_limit.py`:

```python
"""
Konfigurasi rate limiting per alamat IP.

Modul ini hanya menentukan KUNCI pembatas, angka limit, dan bentuk respons 429.
Endpoint mana yang dibatasi ditentukan di router masing-masing (spec 5.3).
"""

import os
from datetime import datetime, timezone

from fastapi import Request
from fastapi.responses import JSONResponse
from slowapi import Limiter
from slowapi.errors import RateLimitExceeded
from slowapi.util import get_remote_address

_NILAI_BENAR = ("1", "true", "yes", "on")


def rate_limit_enabled() -> bool:
    return os.getenv("RATE_LIMIT_ENABLED", "true").strip().lower() in _NILAI_BENAR


def _trust_forwarded_for() -> bool:
    return (
        os.getenv("RATE_LIMIT_TRUST_FORWARDED_FOR", "false").strip().lower()
        in _NILAI_BENAR
    )


def client_key(request: Request) -> str:
    """
    Kunci pembatas: alamat IP klien.

    X-Forwarded-For hanya dipercaya bila RATE_LIMIT_TRUST_FORWARDED_FOR=true.
    Default sengaja false: header ini dapat dipalsukan klien, dan memercayainya
    tanpa proxy tepercaya justru membuat limit dapat dilewati sepenuhnya.
    """
    if _trust_forwarded_for():
        forwarded = request.headers.get("X-Forwarded-For", "")
        if forwarded:
            return forwarded.split(",")[0].strip()
    return get_remote_address(request)


def generate_limit() -> str:
    return os.getenv("RATE_LIMIT_GENERATE", "10/minute")


def admin_verify_limit() -> str:
    return os.getenv("RATE_LIMIT_ADMIN_VERIFY", "5/minute")


limiter = Limiter(key_func=client_key, enabled=rate_limit_enabled())


def _retry_after_seconds(exc: RateLimitExceeded, default: int = 60) -> int:
    """Panjang jendela limit dari exception slowapi, dengan cadangan yang aman."""
    inner = getattr(getattr(exc, "limit", None), "limit", None)
    get_expiry = getattr(inner, "get_expiry", None)
    if callable(get_expiry):
        try:
            return int(get_expiry())
        except Exception:
            return default
    return default


def rate_limit_exceeded_handler(request: Request, exc: RateLimitExceeded) -> JSONResponse:
    """Amplop error standar API_CONTRACT.md, ditambah header Retry-After."""
    retry_after = _retry_after_seconds(exc)
    return JSONResponse(
        status_code=429,
        content={
            "detail": (
                "Terlalu banyak permintaan dari alamat ini. "
                f"Silakan coba lagi dalam {retry_after} detik."
            ),
            "error_code": "RATE_LIMIT_EXCEEDED",
            "timestamp": datetime.now(timezone.utc).isoformat(),
        },
        headers={"Retry-After": str(retry_after)},
    )
```

- [ ] **Step 5: Daftarkan limiter di `main.py`**

Pada `v2/msf-db/backend/app/main.py`, tambahkan import:

```python
from slowapi.errors import RateLimitExceeded

from app.utils.rate_limit import limiter, rate_limit_exceeded_handler
```

lalu setelah baris `app.add_middleware(APIKeyMiddleware)`:

```python
# Rate limiting per IP. Endpoint yang dibatasi ditandai dekorator di routernya.
app.state.limiter = limiter
app.add_exception_handler(RateLimitExceeded, rate_limit_exceeded_handler)
```

- [ ] **Step 6: Pasang dekorator dan `error_code` di `generate.py`**

slowapi mensyaratkan endpoint memiliki parameter bernama tepat `request` bertipe `Request`. Nama itu saat ini dipakai oleh body Pydantic, sehingga body diganti nama menjadi `payload`.

Pada `v2/msf-db/backend/app/routers/generate.py`, tambahkan import:

```python
from fastapi import Request

from app.utils.rate_limit import generate_limit, limiter
```

Untuk `generate_from_ddl`, ganti dekorator dan tanda tangan fungsi:

```python
@router.post("/generate/from-ddl", response_model=GenerateJobResponse)
@limiter.limit(generate_limit)
async def generate_from_ddl(
    request: Request,
    payload: GenerateFromDDLRequest,
    background_tasks: BackgroundTasks,
):
```

lalu ganti SELURUH kemunculan `request.` di dalam badan fungsi tersebut menjadi `payload.` (`request.sql_content`, `request.project_name`, `request.ai_provider`, `request.model`, `request.language`, `request.detail_level`, `request.business_context`, `request.author`, `request.output_format`), termasuk pada baris `logger.info(...)` di awal fungsi.

Lakukan hal yang sama untuk `generate_from_db`:

```python
@router.post("/generate/from-db", response_model=GenerateJobResponse)
@limiter.limit(generate_limit)
async def generate_from_db(
    request: Request,
    payload: GenerateFromDBRequest,
    background_tasks: BackgroundTasks,
):
```

dengan mengganti seluruh `request.` menjadi `payload.` di badan fungsinya, termasuk `payload_dict = payload.model_dump() ...` dan `DBConnector.test_connection(payload.connection)`. Perhatikan variabel lokal bernama `payload_dict` yang sudah ada — jangan tertukar.

Terakhir, pada KEDUA fungsi, ganti gerbang konkurensi agar membawa `error_code`:

```python
    if len(active_jobs) >= max_concurrent:
        raise AppDetailedException(
            detail=(
                f"Terlalu banyak job aktif ({len(active_jobs)}/{max_concurrent}). "
                "Tunggu job sebelumnya selesai."
            ),
            status_code=429,
            error_code="JOB_QUEUE_FULL",
        )
```

- [ ] **Step 7: Pasang dekorator di `/api/admin/verify`**

Pada `v2/msf-db/backend/app/routers/admin.py`, tambahkan import:

```python
from fastapi import Request

from app.utils.rate_limit import admin_verify_limit, limiter
```

lalu ganti endpoint `verify_passcode`:

```python
@router.post("/verify", dependencies=[Depends(verify_admin_passcode)])
@limiter.limit(admin_verify_limit)
async def verify_passcode(request: Request):
    """Endpoint sederhana untuk memvalidasi passcode dari frontend."""
    return {"success": True, "message": "Otentikasi admin berhasil."}
```

- [ ] **Step 8: Jalankan test rate limit untuk memastikan lulus**

```bash
cd "C:/Users/ROG/Documents/query db/v2/msf-db/backend" && python -m pytest tests/test_rate_limit.py -v
```

Expected: 6 PASS.

- [ ] **Step 9: Jalankan seluruh suite**

```bash
cd "C:/Users/ROG/Documents/query db/v2/msf-db/backend" && python -m pytest -q
```

Expected: PASS seluruhnya. Bila test lama gagal karena penggantian nama `request` menjadi `payload`, perbaiki test tersebut — bentuk JSON body TIDAK berubah, jadi kegagalan seperti itu menandakan ada `request.` yang terlewat diganti.

- [ ] **Step 10: Commit**

```bash
cd "C:/Users/ROG/Documents/query db" && git add v2/msf-db/backend && git commit -m "feat: tambah rate limiting per IP"
```

---

## Task 8: Bedakan dua jenis 429 di frontend

Spec 11.3 (sisi frontend), 9.5, 9.6, 8.2.

**Files:**
- Modify: `v2/msf-db/frontend/src/lib/api.ts:218-235`

**Interfaces:**
- Consumes: `error_code` `RATE_LIMIT_EXCEEDED` dan `JOB_QUEUE_FULL` dari Task 7, serta `RESULT_EXPIRED` dari Task 6. `ApiError.errorCode` sudah tersedia di `api.ts:24`.
- Produces: pesan pengguna yang berbeda untuk kedua kondisi 429.

Frontend TIDAK memiliki kerangka pengujian sama sekali (tidak ada jest, vitest, maupun playwright di `package.json`). Ini batasan yang diakui terbuka pada spec 17, sehingga task ini diverifikasi lewat lint, build, dan pemeriksaan manual.

- [ ] **Step 1: Tambahkan cabang 429 dan 410**

Pada `v2/msf-db/frontend/src/lib/api.ts`, di dalam `getFriendlyErrorMessage`, sisipkan setelah baris penanganan 422:

```typescript
    if (err.status === 410) {
      return (
        err.message ||
        "Berkas hasil sudah kedaluwarsa dan dihapus dari server. Silakan jalankan ulang proses generate."
      );
    }
    if (err.status === 429) {
      if (err.errorCode === "JOB_QUEUE_FULL") {
        return (
          err.message ||
          "Antrean pekerjaan sedang penuh. Tunggu pekerjaan lain selesai lalu coba lagi."
        );
      }
      return "Terlalu sering mengirim permintaan. Mohon tunggu sebentar sebelum mencoba lagi.";
    }
```

- [ ] **Step 2: Verifikasi lint dan build**

```bash
cd "C:/Users/ROG/Documents/query db/v2/msf-db/frontend" && npm run lint && npm run build
```

Expected: keduanya lulus tanpa error.

- [ ] **Step 3: Verifikasi manual dua pesan 429**

Jalankan backend, lalu:

1. Setel `RATE_LIMIT_GENERATE=2/minute` dan restart backend. Tekan Generate empat kali beruntun. Expected: toast berbunyi "Terlalu sering mengirim permintaan...".
2. Setel `MAX_CONCURRENT_JOBS=1` dan `RATE_LIMIT_ENABLED=false`, restart, jalankan satu job panjang lalu tekan Generate lagi. Expected: toast berbunyi tentang antrean penuh, BUKAN pesan "terlalu sering".

Catat hasil kedua pemeriksaan di task report Task 10. Bila salah satu tidak dapat dijalankan, laporkan apa adanya — jangan diklaim lulus.

- [ ] **Step 4: Commit**

```bash
cd "C:/Users/ROG/Documents/query db" && git add v2/msf-db/frontend/src/lib/api.ts && git commit -m "feat: bedakan penanganan 429 di frontend"
```

---

## Task 9: Aktifkan dukungan SQL Server

Spec 11.4, F02, 9.17, risiko bagian 20.

**Files:**
- Modify: `v2/msf-db/backend/requirements.txt:34`
- Modify: `v2/msf-db/backend/Dockerfile`
- Modify: `v2/msf-db/backend/app/services/db_connector.py`
- Test: `v2/msf-db/backend/tests/test_db_connector_sqlserver.py` (baru)

**Interfaces:**
- Consumes: variabel env `MSSQL_ODBC_DRIVER` dan `MSSQL_TRUST_SERVER_CERTIFICATE` dari Task 2.
- Produces: cabang `sqlserver` pada `build_connection_url`, `_filter_system_schemas`, `_get_version_query`, dan default schema `dbo`.

- [ ] **Step 1: Tulis test yang gagal**

Buat `v2/msf-db/backend/tests/test_db_connector_sqlserver.py`:

```python
"""Dukungan SQL Server: pembentukan URL dan penyaringan skema sistem.

Batasan yang diakui terbuka (spec 17): koneksi SQL Server sungguhan tidak dapat
diuji tanpa server dan driver ODBC terpasang.
"""

from app.models.schemas import DBConnection
from app.services.db_connector import DBConnector


def _conn(**overrides) -> DBConnection:
    data = {
        "engine": "sqlserver",
        "host": "db.internal",
        "port": 1433,
        "database": "penjualan",
        "username": "sa",
        "password": "rahasia",
    }
    data.update(overrides)
    return DBConnection(**data)


def test_build_connection_url_memakai_driver_18(monkeypatch):
    monkeypatch.delenv("MSSQL_ODBC_DRIVER", raising=False)

    url = DBConnector.build_connection_url(_conn())

    assert url.startswith("mssql+pyodbc://sa:rahasia@db.internal:1433/penjualan")
    assert "driver=ODBC+Driver+18+for+SQL+Server" in url


def test_build_connection_url_menyertakan_trust_server_certificate(monkeypatch):
    monkeypatch.delenv("MSSQL_TRUST_SERVER_CERTIFICATE", raising=False)

    url = DBConnector.build_connection_url(_conn())

    assert "TrustServerCertificate=yes" in url


def test_driver_dan_trust_dapat_diubah_lewat_env(monkeypatch):
    monkeypatch.setenv("MSSQL_ODBC_DRIVER", "ODBC Driver 17 for SQL Server")
    monkeypatch.setenv("MSSQL_TRUST_SERVER_CERTIFICATE", "no")

    url = DBConnector.build_connection_url(_conn())

    assert "driver=ODBC+Driver+17+for+SQL+Server" in url
    assert "TrustServerCertificate=no" in url


def test_port_bawaan_sqlserver_adalah_1433():
    url = DBConnector.build_connection_url(_conn(port=None))

    assert "@db.internal:1433/" in url


def test_filter_system_schemas_membuang_skema_bawaan_sqlserver():
    schemas = [
        "dbo",
        "penjualan",
        "sys",
        "INFORMATION_SCHEMA",
        "guest",
        "db_owner",
        "db_accessadmin",
        "db_securityadmin",
        "db_ddladmin",
        "db_backupoperator",
        "db_datareader",
        "db_datawriter",
        "db_denydatareader",
        "db_denydatawriter",
    ]

    hasil = DBConnector._filter_system_schemas(schemas, "sqlserver")

    assert hasil == ["dbo", "penjualan"]


def test_version_query_sqlserver():
    assert DBConnector._get_version_query("sqlserver") == "SELECT @@VERSION"
```

- [ ] **Step 2: Jalankan test untuk memastikan gagal**

```bash
cd "C:/Users/ROG/Documents/query db/v2/msf-db/backend" && python -m pytest tests/test_db_connector_sqlserver.py -v
```

Expected: FAIL — URL masih memuat `ODBC+Driver+17` dan tanpa `TrustServerCertificate`.

- [ ] **Step 3: Ubah `db_connector.py`**

Pada `v2/msf-db/backend/app/services/db_connector.py`, tambahkan `import os` di blok import bagian atas (saat ini belum ada).

Ganti cabang `sqlserver` di `build_connection_url`:

```python
        elif engine == DBEngine.SQLSERVER or engine == "sqlserver":
            # Driver 18 mewajibkan enkripsi secara bawaan dan menolak server tanpa
            # sertifikat tepercaya, karena itu TrustServerCertificate dibuat eksplisit.
            driver = quote_plus(
                os.getenv("MSSQL_ODBC_DRIVER", "ODBC Driver 18 for SQL Server")
            )
            trust = os.getenv("MSSQL_TRUST_SERVER_CERTIFICATE", "yes")
            return (
                f"mssql+pyodbc://{username_enc}:{password_enc}"
                f"@{conn.host}:{port}/{conn.database}"
                f"?driver={driver}&TrustServerCertificate={trust}"
            )
```

Pada `_filter_system_schemas`, tambahkan entri kamus dan cabangnya:

```python
        system_schemas = {
            "postgresql": ["information_schema", "pg_catalog", "pg_toast", "pg_temp_1", "pg_toast_temp_1"],
            "mysql": ["information_schema", "performance_schema", "sys", "mysql"],
            "sqlite": ["main"],
            "sqlserver": [
                "sys", "information_schema", "guest", "db_owner", "db_accessadmin",
                "db_securityadmin", "db_ddladmin", "db_backupoperator",
                "db_datareader", "db_datawriter", "db_denydatareader",
                "db_denydatawriter",
            ],
        }
```

dan di dalam loop, setelah cabang `mysql`:

```python
            if engine == "sqlserver" and s_lower in system_schemas["sqlserver"]:
                continue
```

Pada `_get_version_query`, tambahkan cabang eksplisit sebelum `else`:

```python
        elif engine_type == DBEngine.SQLSERVER or engine_type == "sqlserver":
            return "SELECT @@VERSION"
```

Pada `test_connection_sync`, ganti penentuan schema bawaan:

```python
                default_schema = {"sqlite": "main", "sqlserver": "dbo"}.get(
                    str(conn.engine), "public"
                )
                active_schema = conn.schema_name or default_schema
```

- [ ] **Step 4: Jalankan test untuk memastikan lulus**

```bash
cd "C:/Users/ROG/Documents/query db/v2/msf-db/backend" && python -m pytest tests/test_db_connector_sqlserver.py -v
```

Expected: 6 PASS.

- [ ] **Step 5: Aktifkan `pyodbc`**

Pada `v2/msf-db/backend/requirements.txt`, baris 34, hilangkan tanda komentar:

```
pyodbc==5.2.0                   # SQL Server (butuh msodbcsql18 di image)
```

`pymongo` pada baris berikutnya TETAP dikomentari — MongoDB di luar cakupan rilis ini (spec bagian 4).

- [ ] **Step 6: Pasang driver ODBC di Dockerfile**

Pada `v2/msf-db/backend/Dockerfile`, ganti blok `RUN apt-get update && apt-get install -y ...` yang ada dengan:

```dockerfile
# Install system dependencies
# - curl: untuk healthcheck
# - gcc + libpq-dev: untuk psycopg2
# - unixodbc-dev + msodbcsql18: untuk pyodbc (SQL Server)
# Repositori Microsoft diverifikasi lewat kunci GPG, bukan apt-key yang usang.
RUN apt-get update && apt-get install -y --no-install-recommends \
    curl \
    gcc \
    gnupg \
    ca-certificates \
    libpq-dev \
    unixodbc-dev \
    && curl -fsSL https://packages.microsoft.com/keys/microsoft.asc \
       | gpg --dearmor -o /usr/share/keyrings/microsoft-prod.gpg \
    && echo "deb [arch=amd64,arm64 signed-by=/usr/share/keyrings/microsoft-prod.gpg] https://packages.microsoft.com/debian/12/prod bookworm main" \
       > /etc/apt/sources.list.d/mssql-release.list \
    && apt-get update \
    && ACCEPT_EULA=Y apt-get install -y --no-install-recommends msodbcsql18 \
    && rm -rf /var/lib/apt/lists/*
```

- [ ] **Step 7: Verifikasi image dapat di-build**

```bash
cd "C:/Users/ROG/Documents/query db/v2/msf-db" && docker compose build backend
```

Expected: build sukses. Bila gagal pada arsitektur mesin ini (risiko yang sudah dicatat spec bagian 20), kembalikan `pyodbc` menjadi komentar, kembalikan blok Dockerfile, dan LAPORKAN kegagalannya apa adanya beserta keluaran build — jangan diklaim berhasil.

- [ ] **Step 8: Verifikasi driver terpasang di dalam image**

```bash
cd "C:/Users/ROG/Documents/query db/v2/msf-db" && docker compose run --rm --entrypoint "" backend python -c "import pyodbc; print(pyodbc.drivers())"
```

Expected: keluaran memuat `ODBC Driver 18 for SQL Server`.

- [ ] **Step 9: Jalankan seluruh suite**

```bash
cd "C:/Users/ROG/Documents/query db/v2/msf-db/backend" && python -m pytest -q
```

Expected: PASS seluruhnya.

- [ ] **Step 10: Commit**

```bash
cd "C:/Users/ROG/Documents/query db" && git add v2/msf-db/backend && git commit -m "feat: aktifkan dukungan SQL Server"
```

---

## Task 10: Dokumentasi operations, ADR-005, dan sinkronisasi dokumentasi wajib

Spec bagian 14, 15, 21; AGENTS.md aturan 7, 8, 11, 12, 13.

**Files:**
- Create: `v2/msf-db/backend/docs/operations/job-database-backup.md`
- Create: `v2/msf-db/backend/docs/operations/scheduler-cleanup.md`
- Create: `v2/msf-db/backend/docs/operations/rate-limiting.md`
- Create: `v2/msf-db/dev-docs/decisions/005-persistent-job-queue-sqlite.md`
- Create: `v2/msf-db/dev-docs/commit-logs/2026-08-03.md`
- Create: `v2/msf-db/planning/modules.md`, `v2/msf-db/planning/timeline.md`
- Create: `v2/msf-db/reports/task/2026-08-03-msf-db-v2.2.0.md`
- Modify: `v2/msf-db/dev-docs/decisions/004-in-memory-job-queue.md`, `dev-docs/ai/VERSION.md`, `MODULE_MAP.md`, `CURRENT_STATE.md`, `TECHNICAL_DEBT.md`, `KNOWN_ISSUES.md`, `TASKS.md`, `FINAL_SYSTEM_HANDOVER.md`, `dev-docs/architecture/database.md`, `dev-docs/architecture/backend-structure.md`, `dev-docs/CHANGELOG.md`, `planning/database.md`, `v2/msf-db/claude.md`, `v2/msf-db/TODO.md`

**Interfaces:**
- Consumes: seluruh perubahan Task 1 sampai 9.
- Produces: dokumentasi yang menjadi sumber bacaan sesi berikutnya. Tidak ada kode yang bergantung padanya.

- [ ] **Step 1: Baca template sebelum menulis**

```bash
cd "C:/Users/ROG/Documents/query db/v2/msf-db" && ls ai-rules/operations/_templates/ ai-rules/dev-docs-ai-templates/ ai-rules/planning-templates/ ai-rules/reports-templates/ ai-rules/decisions/
```

`ai-rules/` IMMUTABLE — hanya dibaca. Seluruh keluaran ditulis ke `backend/docs/operations/`, `dev-docs/`, `planning/`, dan `reports/`.

- [ ] **Step 2: Tulis tiga dokumen operations**

Ikuti struktur wajib AGENTS.md aturan 11: Apa, Kenapa, Prerequisites, Step-by-step setup, Verifikasi, Troubleshooting, Maintenance.

Berkas ini masuk branch `main` bersama kode, sehingga aturan 14 berlaku: DILARANG memuat kredensial aktual, hanya boleh merujuk `.env`.

- `job-database-backup.md` — cara backup volume `msf2_backend_outputs`, frekuensi yang disarankan, cara restore, cara memverifikasi hasil restore, cara memperbaiki kepemilikan direktori bila volume terlanjur dibuat manual sebagai root, dan cara membersihkan berkas yatim peninggalan sebelum v2.2.0 secara manual (spec 9.16).
- `scheduler-cleanup.md` — dua pembersihan yang berjalan tiap 15 menit, apa yang dihapus masing-masing, cara mengubah `MAX_JOB_RETENTION_MINUTES` dan `JOB_RECORD_RETENTION_DAYS`, cara memverifikasi keduanya berjalan lewat log, cara menghentikan sementara.
- `rate-limiting.md` — cara menyetel `RATE_LIMIT_GENERATE` dan `RATE_LIMIT_ADMIN_VERIFY`, kapan `RATE_LIMIT_TRUST_FORWARDED_FOR` boleh dinyalakan dan bahayanya bila salah, cara mematikan cepat saat insiden lewat `RATE_LIMIT_ENABLED=false`.

- [ ] **Step 3: Terbitkan ADR-005 dan supersede ADR-004**

Buat `v2/msf-db/dev-docs/decisions/005-persistent-job-queue-sqlite.md` berstatus `Accepted`, memuat: konteks konflik fondasi (spec bagian 2), keputusan bahwa PRD menang, konsekuensi (retensi dua tingkat, rekonsiliasi startup, `list_jobs()` tetap dari memori), dan alternatif yang ditolak.

Pada `v2/msf-db/dev-docs/decisions/004-in-memory-job-queue.md`, ubah baris status menjadi:

```markdown
**Status:** Superseded by ADR-005
```

Isi ADR-004 lainnya TIDAK dihapus — jejak keputusan harus tetap utuh.

- [ ] **Step 4: Perbarui dokumen `dev-docs/ai/`**

| Berkas | Isi perubahan |
|---|---|
| `VERSION.md` | v2.1.0 menjadi v2.2.0; isi ulang target rilis berikutnya |
| `MODULE_MAP.md` | Tambah `background/job_store.py` dan `utils/rate_limit.py` (spec 5.2) |
| `CURRENT_STATE.md` | Keadaan setelah rilis |
| `TECHNICAL_DEBT.md` | Tambah TD-005 sampai TD-007 (spec 12.2) dan debt `DocxExporter.export()` memblokir event loop |
| `KNOWN_ISSUES.md` | Tambah absennya HTTP security headers (spec 13), absennya batas sumber daya container (D-1), dan temuan 9.14 sampai 9.17 |
| `TASKS.md` | Tandai selesai |
| `FINAL_SYSTEM_HANDOVER.md` | Perbarui; DILARANG memuat kredensial, IP server, atau secret |

- [ ] **Step 5: Perbarui arsitektur, changelog, dan planning**

- `dev-docs/architecture/database.md` — sebelumnya menyatakan tidak ada penyimpanan persisten; masukkan skema `jobs` dan alasan tiap keputusan skema (spec 6.2).
- `dev-docs/architecture/backend-structure.md` — komponen `background/job_store.py` dan batas tanggung jawabnya (spec 5.3).
- `dev-docs/CHANGELOG.md` — entri v2.2.0. WAJIB memuat catatan bahwa statistik Admin Portal berubah makna: kini melintasi 30 hari, bukan 60 menit terakhir (spec 8.3).
- `dev-docs/commit-logs/2026-08-03.md` — detail seluruh commit Task 1 sampai 10.
- `planning/database.md` — bagian 1 masih menyatakan penyimpanan in-memory; perbarui.
- `planning/modules.md` dan `planning/timeline.md` — ditandai wajib oleh template tetapi belum ada; buat dari `ai-rules/planning-templates/`.

- [ ] **Step 6: Perbarui `claude.md` dan `TODO.md`**

`v2/msf-db/claude.md` memuat bagian berjudul "Known Issues Fixed in v2" yang menyesatkan — pada saat ditulis, tidak satu pun sudah dikerjakan (spec 1.3). Perbaiki status keenam poin agar mencerminkan keadaan sesudah rilis ini, dan perbaiki dua klaim yang tetap tidak benar: halaman Settings tetap redirect (keputusan UX 2026-07-06), dan poin download memang sudah benar sejak awal.

Pada `v2/msf-db/TODO.md`, tandai selesai item persistent job queue, rate limiting, dan SQL Server.

- [ ] **Step 7: Usulkan (JANGAN terapkan) perubahan PRD dan README**

Tiga hal berikut memerlukan persetujuan user dan hanya boleh DIUSULKAN sebagai draft di task report:

1. `planning/prd.md` — tambah NFR-R1, NFR-S1, NFR-S2, NFR-O1 (spec 3.3). PRD adalah cetak biru, bukan living document.
2. `backend/README.md` dan `frontend/README.md` — AGENTS.md aturan 8.

- [ ] **Step 8: Tulis task report**

Buat `v2/msf-db/reports/task/2026-08-03-msf-db-v2.2.0.md` mengikuti `ai-rules/TASK_REPORT_TEMPLATE.md` dan aturan `ai-rules/TASK_REPORT_RULES.md`, memuat:

1. Summary (1-5 poin)
2. Files changed (daftar lengkap)
3. Verify commands beserta keluaran sebenarnya
4. Merge steps
5. Hasil Security Pre-Merge Checklist (`ai-rules/security/` Part I)
6. Pelanggaran standar yang dilaporkan tetapi TIDAK dikerjakan: `routers/generate.py` melebihi batas route (spec 12.1), `DiagramCanvas.tsx`, `admin/page.tsx`, `diagram/templates.ts` melebihi batas view (spec 12.2), absennya HTTP security headers (spec 13), temuan 9.14 sampai 9.17
7. Batasan pengujian yang diakui terbuka: koneksi SQL Server sungguhan tidak diuji; frontend tidak memiliki infrastruktur pengujian sehingga penanganan 429 diverifikasi manual
8. Hasil pengukuran `docker stats` dari Step 10

- [ ] **Step 9: Jalankan seluruh kriteria penerimaan spec bagian 18**

Jalankan setiap butir dan catat hasilnya. Yang perlu dibuktikan secara manual di luar test suite:

```bash
cd "C:/Users/ROG/Documents/query db/v2/msf-db" && docker compose up -d && docker compose restart backend && docker volume ls | grep msf
```

Expected: dua set volume terpisah (`msf_*` milik msf-app dan `msf2_*` milik v2); job yang dibuat sebelum restart masih dapat ditemukan lewat `access_code`; job yang sedang berjalan saat restart berstatus `error` dengan pesan restart.

```bash
cd "C:/Users/ROG/Documents/query db/v2/msf-db" && grep -rn "admin123" --exclude-dir=node_modules --exclude-dir=.git . | head
```

Expected: tidak ada keluaran.

- [ ] **Step 10: Ukur konsumsi sumber daya (D.4, D-4)**

```bash
docker stats --no-stream
```

Jalankan tiga kali: saat sistem diam, saat satu pekerjaan berjalan, dan saat v2 berjalan bersamaan dengan msf-app. Catat angkanya di task report menggantikan perkiraan pada spec D.4, dan periksa ukuran `jobs.db` untuk memvalidasi perkiraan 1 KB per baris.

- [ ] **Step 11: Verifikasi akhir menyeluruh**

```bash
cd "C:/Users/ROG/Documents/query db/v2/msf-db/backend" && python -m pytest -v
```

```bash
cd "C:/Users/ROG/Documents/query db/v2/msf-db/frontend" && npm run lint && npm run build
```

```bash
cd "C:/Users/ROG/Documents/query db/v2/msf-db" && docker compose config
```

Bila salah satu gagal: TIDAK BOLEH merge. Laporkan kegagalan beserta keluarannya apa adanya.

- [ ] **Step 12: Commit**

```bash
cd "C:/Users/ROG/Documents/query db" && git add v2/msf-db && git commit -m "docs: sinkronisasi planning, dev-docs, ADR-005, dan operations"
```

- [ ] **Step 13: Serahkan push ke manusia**

DILARANG `git push`. Sampaikan ke user daftar commit yang siap di-push beserta perintah yang harus mereka jalankan sendiri, dan langkah merge `dev` ke `main` yang mengecualikan `ai-rules/`, `dev-docs/`, `planning/`, `reports/`, dan `AGENTS.md` (AGENTS.md bagian 5).

---

## Catatan keputusan yang menunggu user

| # | Butir | Terdampak | Rujukan |
|---|---|---|---|
| 1 | Izin mengubah `.env` | Task 2 Step 1 dan 5 | AGENTS.md aturan 3 |
| 2 | Lokasi spesifikasi: tetap di `docs/superpowers/specs/` atau dipindah ke `v2/msf-db/planning/` | Task 10 | spec 15.2 |
| 3 | Persetujuan penambahan NFR-R1, NFR-S1, NFR-S2, NFR-O1 ke `planning/prd.md` | Task 10 Step 7 | spec 3.3 |
| 4 | Persetujuan draft perubahan `backend/README.md` dan `frontend/README.md` | Task 10 Step 7 | AGENTS.md aturan 8 |

Butir 2, 3, dan 4 tidak memblokir Task 1 sampai 9. Butir 1 memblokir Task 2 Step 5 saja.
